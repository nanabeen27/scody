# Scody Design System

스코디의 **단일 디자인 기준**. 모든 페이지·컴포넌트는 이 문서를 따른다.
참고: VoltAgent/awesome-design-md 의 Notion·Linear·Mintlify 디자인. 복제가 아니라 아래처럼 재조합한다.

- **Notion** — 따뜻하고 차분한 중립 색, 높은 가독성, 부드러운 표면
- **Linear** — 정밀한 간격, 명확한 상태 표현, 절제된 인터페이스
- **Mintlify** — 명확한 정보 계층과 탐색 구조, 읽기 최적화
- **스코디** — 한국 고등학생 학습 서비스다운 친근함 + 상용 제품 수준의 일관성

> 대상 플랫폼: Expo + React Native Web(웹 우선). 색은 CSS 변수(`--sc-*`)로 라이트/다크 전환.

---

## 1. Product design principles

1. **정보 계층이 장식보다 우선.** 한 화면에는 하나의 주인공만.
2. **학습 흐름 우선.** 디자인은 "오늘 뭘 할지"를 3초 안에 답해야 한다.
3. **절제.** 색·그림자·라운드·아이콘은 기본을 낮추고 필요할 때만 쓴다.
4. **일관성.** 같은 의미는 항상 같은 토큰으로 표현한다.
5. **정직한 상태.** 로딩·빈·오류·성공을 숨기지 않고 조용히 보여준다.
6. **친근하지만 과하지 않게.** 사람이 쓴 짧은 한국어. 과장·전문용어 금지.

## 2. Visual direction

- 중립 쿨 그레이 배경 + 절제된 Toss 블루 단색 강조(주요 행동/선택에만).
- 텍스트 타이포그래피가 화면을 이끈다. 카드는 "구분이 필요한 곳"에만.
- 경계는 얇은 hairline, 표면 차이는 밝기로. 큰 그림자로 띄우지 않는다.
- 좌측 정렬 기본. 본문은 읽기 좋은 폭으로 제한.

## 3. Semantic color tokens

정의: `src/theme/palette.ts` (라이트/다크). 코드에서는 항상 `colors.<name>`(=`var(--sc-<name>)`)로 참조.

| 토큰 | 역할 |
|---|---|
| `bg` | 페이지 배경 |
| `surface` | 카드/입력 표면 |
| `offset` | 은은한 면(지표/선택 배경) |
| `hover` | hover/press 배경 |
| `notice` | 상시 고지 면(대리 보기 배너). 오답·오류의 `danger`와 갈라 둔다 |
| `border` | 기본 경계(hairline) |
| `borderStrong` | 강한 경계/입력 테두리 |
| `ink` | 본문 텍스트 |
| `inkSecondary` | 보조 텍스트 |
| `inkTertiary` | 캡션·플레이스홀더. **읽어야 하는 문장에는 쓰지 않는다** — 아래 참고 |
| `accent` | 주요 버튼·선택·활성·링크 |
| `accentText` | accent 위 텍스트 |
| `accentSoft` | 선택 항목의 은은한 배경 |
| `success` | 정답·완료 |
| `danger` | 오답·오류·파괴적 행동 |
| `personal` | 개인 학습 출처(점/라벨) |
| `academy` | 학원 과제 출처(점/라벨) |
| `shadow` | 떠 있는 면의 그림자(다크는 값이 다르다) |
| `kakao`/`kakaoText` | 카카오 버튼(브랜드 고정) |

**실제 색 값은 여기 적지 않는다. 단일 소스는 `src/theme/palette.ts`다.**
값을 두 곳에 두면 한쪽만 바뀌고, 실제로 이 표는 모든 행이 낡아 있었다(파랑 `#3182f6`을 적어 두고
코드는 청록 `#20808d`을 쓰고 있었다). 낡을 수 없는 표가 낡은 표보다 낫다.

**`inkTertiary`는 본문 대비 AA에 미달한다**(D-166). 실측: 라이트에서 `bg` 위 **2.96:1** ·
`surface` 위 3.20:1 · `offset` 위 2.71:1, 다크에서 4.43:1. 기준은 4.5:1이다. 그래서 **읽어야
하는 문장에는 쓰지 않는다** — 고지·안내·출처·권한 설명·`예시 화면` 표기는 `inkSecondary`다.
`tertiary`가 남는 자리는 장식이나 목업 안의 그림 글자다. 같은 이유로 §14-1이 `Row`의 `meta`에
값을 두지 않고, 여기 §16이 `eyebrow`를 한글 경로 표시에 쓰지 않는다. 토큰 값 자체를 어떻게 할지는
A-123에 열려 있다.

**불투명도로 글자를 흐리지 않는다.** `opacity`는 대비를 그만큼 곱해 버린다 — 강조색 12px에 0.6을
주니 2.25:1, 흰 글자에 0.9를 주니 4.63 → 4.08:1이었다(둘 다 D-166에서 걷어냈다). 흐리게 두고
싶으면 색 토큰을 바꾼다.

**상시 고지 면은 면·선·아이콘 셋으로 구분한다**(D-147). 화면에 계속 떠 있는 고지(지금은 대리 보기
배너 하나)는 ①`notice` 면 ②`borderStrong` 아래 테두리 ③`alert-circle` 아이콘을 함께 쓴다.
`danger`는 쓰지 않는다 — 오답·오류의 색이라 상시 배너에 쓰면 그 신호가 죽는다(D-071).
`offset`으로도 두지 않는다: 본문과 라이트 1.09:1 · 다크 1.21:1이라 면으로는 구분이 없어서 1px
테두리가 구분을 혼자 지고 있었다(A-080). `notice`는 본문과 **라이트 1.48:1 · 다크 1.70:1**이고,
그 면 위에서 `ink`(11.26:1 / 9.51:1)와 `inkSecondary`(4.59:1 / 5.01:1) 둘 다 AA다.

규칙: **accent = Perplexity True Turquoise `#20808d`(다크 `#3aa7b1`) 단색.** 파랑/보라·그라데이션 금지. 배경은 Perplexity Paper(`#fbfaf4`)/Offblack(`#091717`), 텍스트 Offblack. 강조색은 주요 버튼·선택·활성 탭·링크에만. 출처색: 개인=Turquoise700 `#114f56`, 학원=Terra Cotta `#a84b2f`. **실제 값의 단일 소스는 `src/theme/palette.ts`** (위 표가 다르면 palette.ts가 우선).

## 4. Typography scale

- **폰트: 본문·제목 전부 Pretendard.** 무게 4종(Regular 400 · Medium 500 · SemiBold 600 · Bold 700). 정의는 `src/theme/tokens.ts`의 `typeface`, 로드는 `app/_layout.tsx`의 `useFonts`.
  - 파일은 레포에 번들한다(SIL OFL 1.1, `assets/fonts/Pretendard-LICENSE.txt`). CDN·네트워크 의존 없음.
  - **플랫폼마다 파일이 다르다(D-053).** 네이티브는 원본 `assets/fonts/Pretendard-*.ttf`(약 10.7MB, 앱 번들이라 내려받지 않는다), 웹은 서브셋 `assets/fonts/web/ScodyKR-*.woff2`(약 2.5MB). 소스와 패밀리 이름은 `src/theme/fonts.ts` / `fonts.web.ts` 한 쌍에만 두고, 화면 코드는 `typeface.*`로만 참조한다.
  - 웹 서브셋 이름이 `ScodyKR`인 이유: Pretendard 라이선스에 Reserved Font Name `Pretendard`가 걸려 있고 서브셋은 OFL 4항의 Modified Version이라 같은 이름을 쓸 수 없다. 글자 모양은 Pretendard 그대로다.
  - **서브셋에서 한글은 완성형 전체를 남긴다.** 자주 쓰는 2,350자로 줄이면 빠진 음절만 폴백으로 떨어져 한 단어 안에서 서체가 갈린다. 새 기호를 쓰기 시작하면(원문자·괄호·화살표) `scripts/build-web-fonts.py`의 `UNICODES`에 범위를 더하고 다시 만든다.
  - **Space Grotesk는 "Scody" 워드마크에만** 쓴다(`typeface.wordmark`, `src/components/Brand.tsx`). 본문·제목에 쓰지 않는다.
  - 라틴 전용 폰트를 본문에 쓰지 않는다. Inter를 쓰던 동안 한글만 시스템 폰트(맑은 고딕)로 떨어져 한 화면에 두 벌이 섞였다. Pretendard는 한글·라틴을 같은 골격으로 덮는다.
  - 폴백은 `"Apple SD Gothic Neo", "Malgun Gothic", sans-serif`. **로드 실패 시에만 보인다**(D-131) — `app/_layout.tsx`가 `loaded` + `document.fonts.ready`까지 기다린 뒤에야 화면을 그리므로 성공 경로에서는 폴백이 한 프레임도 보이지 않는다. 다만 폰트를 못 받으면(느린 회선·차단) **폴백으로 그린다** — 폰트가 없는 것보다 앱이 안 뜨는 것이 나쁘다.
- **마크(파비콘)**: 청록(`accent`) 라운드 타일 + 워드마크와 같은 골격의 흰 `S`. 32 단위 좌표계, 타일 radius 7.5(약 23%).
  - `S` 윤곽선은 Space Grotesk Bold 글리프에서 뽑은 path다. `public/favicon.svg`(+ 구형 브라우저용 `public/favicon.ico` 16·32·48)에만 있다.
  - **마크는 파비콘 역할만 한다. 앱 화면 안에는 두지 않는다.** 인증 화면·상단바·사이드바 어디서나 워드마크(`Brand`)만 쓴다. 같은 도형을 화면 안에도 두면 로고가 둘이 되고, 글자가 적은 인증 화면에서는 가장 먼저 눈에 띄는 것이 서비스 이름이 아니라 아이콘이 된다.
  - 16px에서 읽히는지 먼저 확인한다. `ㅅ`·`Λ`는 캐럿, `ㅅ+ㅡ`는 eject 기호, 각진 `S`는 숫자 5로 읽혀 모두 버렸다.
- 크기(px): `xs 12 · sm 13 · base 15 · md 16 · lg 18 · xl 22 · xxl 27 · display 34`.
- 변형(`AppText variant`): `display`(히어로 숫자/큰 제목, bold), `title`(xxl bold, 화면 제목), `heading`(xl semibold), `subheading`(md semibold, 섹션), `bodyLg`(md), `body`(base), `label`(base medium), `caption`(sm), `eyebrow`(xs semibold, 자간, 라벨 — 한글에는 쓰지 않는다).
- **크기와 문서 구조는 다른 것이다**(D-166). `variant`는 크기·무게만 정한다. 그 글이 화면의 제목이라는
  사실은 **`headingLevel`**로 따로 말한다(`AppText headingLevel={1}`) — 없으면 스크린리더가 제목으로
  세지 못해 제목 사이를 건너뛸 수 없다(실측: 로그인 전 네 화면에 제목이 0개였다). 화면 제목 1, 섹션 2,
  그 아래 3이고 **단계를 건너뛰지 않는다**. 목업 안의 글자에는 주지 않는다 — 그 화면의 구조가 아니다.
  react-native-web은 `<h1>`이 아니라 `role="heading" aria-level`로 그린다(보조기술에는 같은 것이다).
- 제목 자간은 **-0.2 정도까지만**. 한글은 라틴보다 자간을 좁히면 금방 뭉친다.
- 한글 줄간격: 큰 제목 1.3~1.35 이상, 본문·지문 1.6~1.85. 32px 제목이면 44px, 46px 히어로면 62px.
- 한 화면에 title 1개. 아래로 subheading 섹션 구분. 캡션은 보조 정보에만.

## 5. Spacing and layout system

- 스페이싱(`spacing`): `xs 4 · sm 8 · md 12 · lg 16 · xl 24 · xxl 36 · xxxl 56 · huge 88`.
- 컨텐츠 폭: 본문 `contentMaxWidth 680`, 넓은 화면(학원) `wideMaxWidth 960`, 지문 읽기 `readingMaxWidth 620`.
- 좌측 정렬. 화면은 세로 스택(섹션 간 `xl`). 카드 내부 패딩 `lg~xl`.
- 데스크톱: 좌측 사이드바(`sidebarWidth 248`) + 본문. 모바일: 상단바 + 하단 탭.
- Linear식 정밀함: 같은 맥락의 간격은 항상 같은 토큰. 임의 픽셀 금지.

## 6. Border and radius rules

- radius: `sm 6 · md 8 · lg 12 · xl 18 · card 22 · pill 999`. 기본 요소는 여전히 moderate. **`xl`/`card`는 히어로 카드·플로팅 내비 등 "부드러운 표면" 전용**(실제 Toss 모바일 UI 참고). 일반 카드/버튼/입력에 16px+ 남발 금지 — 큰 라운드는 주인공 표면에만.
- 버튼·입력·작은 요소 `md(8)`, 목록 그룹 `xl(18)`, 히어로 카드 `card(22)`, 토글/점 `pill`.
- 경계는 1px `border`. 구분이 필요한 카드/입력에만. hairline은 리스트 내부 구분선.
- **섹션 사이에는 선을 두지 않는다.** 구분은 여백(`spacing.xl`, 모바일 `lg`)이 한다(§5).
  - 예외는 하나다: **스스로 어떤 경계선도 갖지 않는 블록**(가로 막대 목록·문단 글·캡션)이 연달아 맞닿아 어디서 끊기는지 안 보일 때. 그때만 뒤 섹션에 `Section separated`로 hairline 한 줄을 둔다.
  - `Group`·`Table`·`Passage`처럼 **이미 선을 가진 블록의 앞뒤에는 두지 않는다** — 선이 두 겹으로 보인다. `Table`은 컨테이너 테두리가 없지만 헤더 hairline과 행 구분선이 있어 스스로 덩어리로 읽힌다.
  - 역할에 따라 앞 블록이 달라지면 값도 조건부로 준다(`separated={!isDirector}` — 학원 대시보드 `정답률 분포`).

## 7. Elevation and shadow rules

- **그림자 사용 안 함(기본).** 표면 구분은 `border` + `offset` 밝기 차이로만.
- 떠 있는 요소(드롭다운/모달, **모바일 플로팅 내비**)만 아주 옅은 그림자 1단계 허용. 그 외 금지.
- Glassmorphism·blur·발광 금지.

## 8. Buttons, inputs, cards, navigation patterns

- **Button** (`src/components/Button.tsx`): 높이 **44**(`touch.min`), radius md. `primary`=accent 배경/`accentText`. `secondary`=surface+border. `ghost`=투명. `kakao`=카카오 브랜드. 라벨 medium. 한 화면의 주요 행동 1개만 primary.
  - `size="lg"`(높이 52, radius lg, 라벨 semibold + md)는 **인증 화면과 제출처럼 화면에 할 일이 하나뿐인 곳** 전용. 일반 화면에서 쓰면 위계가 무너진다.
  - `size="sm"`(높이 32, 좌우 패딩 md)는 섹션 제목 옆이나 목록 아래의 보조 행동용.
    - **목록 안에서 줄마다 반복되는 행동에는 `sm`을 쓰지 않는다.** 기본 `md`(44)를 쓴다. 예전에는 `md`가 40이라 화면들이 `style={{ height: 44 }}`로 12곳에서 개별 우회했고(D-084), 그 우회가 두 가지 이름(`tap`·`undoBtn`)으로 갈렸다. 기본값을 고쳐 우회를 없앴다.
  - **버튼은 기본이 전폭이다. 그래서 `hug`이 기본값처럼 쓰인다.** `styles.base`에 폭 제약이 없어 세로 컨테이너의 `alignItems: stretch`를 그대로 받아 컬럼 폭(최대 680)까지 늘어난다.
    - **`fullWidth`나 전폭 방치를 쓸 수 있는 것은 그 화면의 primary 하나뿐이다.** 그 외에는 전부 `hug`. 폭이 화면 끝까지 닿은 버튼은 버튼이 아니라 배너나 구분 블록으로 읽힌다 — 데스크톱 680px에서 특히 그렇다.
    - 판단이 애매하면 이렇게 묻는다: **"이 버튼을 눌러야 화면의 목적이 끝나는가?"** 아니면 `hug`이다. `문제 담으러 가기`·`보러 가기`처럼 **다른 화면으로 보내기만 하는 버튼은 절대 전폭이 아니다.**
    - 반복해서 어긴 규칙이다(A-033). 새 버튼을 놓을 때마다 확인한다.
    - **같은 이름의 같은 행동은 화면이 달라도 같은 무게다.** `문제 담으러 가기`는 다섯 자리(학생 홈 2 · 담아 둔 학습 2 · 기록 1)에서 **강조색 + `hug` + `arrow-right`** 한 벌로 고정한다(D-123). 색을 빼지 않는 이유는 그래도 그 자리의 다음 행동이기 때문이고, 폭을 빼는 이유는 어느 화면에서도 그 화면을 끝내지 않기 때문이다 — 전폭 R1과는 **폭으로** 갈린다.
  - **글자만 있는 행동은 버튼으로 인지되지 않는다.** `ghost`처럼 배경도 테두리도 없는 버튼은 옆의 본문과 구분되지 않는다. 그런 자리에는 **`leading`에 뜻이 통하는 아이콘 하나**를 둔다(`빼기`·`여러 개 빼기` → `minus-circle`, `위로`/`아래로` → `arrow-up`/`arrow-down`, `다시 하기` → `refresh-cw`). 아이콘은 장식이 아니라 "여기는 누르는 곳"이라는 유일한 신호다.
    - 아이콘 색은 라벨과 같은 톤(`inkSecondary`, `tone="accent"`면 `accent`)으로 맞춘다. 크기는 `size="sm"`에 15, 기본 크기에 16.
    - 취소·닫기처럼 뜻이 이미 굳은 라벨에는 두지 않는다 — 아이콘이 늘어나면 어느 것이 진짜 행동인지 흐려진다.
  - **라벨을 둘 자리가 없으면 `IconButton`을 쓴다**(`src/components/IconButton.tsx`). 글리프는 16으로 두고 누름 영역만 44px로 키운다 — `inset`을 주면 커진 만큼 음수 마진으로 되돌려 줄 높이는 그대로다. `hitSlop`은 react-native-web의 `Pressable`에서 듣지 않는다. `★` 같은 글자 글리프를 `Icon` 옆에 두지 않는다 — 굵기가 달라 다른 아이콘 체계로 읽힌다.
  - **눌러도 아무 일이 없는 버튼을 두지 않는다.** 이미 끝난 상태는 버튼이 아니라 글자다(`이해 완료`).
  - **화면의 행동은 `ActionBar` 안에 모은다**(`src/components/ActionBar.tsx`). 버튼을 낱개로 흩어 두면 hug 버튼과 전폭 버튼이 세로로 쌓여 폭이 제각각인 덩어리가 된다.
    - **① 한 줄에 버튼 하나.** 둘을 나란히 두면 어느 것이 주 행동인지 모양으로 알 수 없고, 셋이면 고르기 전에 읽어야 할 것이 셋이 된다. 개발 중에는 둘 이상이면 `console.warn`이 뜬다.
    - **② 주 행동(`primary`)은 좌우로 늘인다.** 강조색이 칠해진 버튼(`시작하기`·`학습 시작하기`·`제출할게요`·`홈으로 갈게요`)은 이 화면에서 할 일 그 자체라 **폭이 곧 위계다**. **늘이는 것은 `Button`이 스스로 한다** — 행동 줄 밖(히어로 카드 안 등)에 있어도 같은 폭이어야 하기 때문이다. 화면마다 `stretch`를 붙이면 같은 종류가 화면마다 다른 폭이 된다(실제로 41개 중 4개만 붙어 있었다). 늘어난 폭도 읽기 폭(680)에서 멈춘다.
      - 구현은 `alignSelf`가 아니라 `width: '100%'`다 — 가로로 놓인 부모 안에서 `alignSelf: stretch`는 **세로**로 늘어나고 폭은 그대로다(히어로 카드가 그랬다).
      - **곁다리 자리의 `primary`는 `hug`으로 뺀다**: 섹션 제목 옆, 목록 줄의 `trailing`, 빈 상태의 다음 행동.
      - **`hug`은 가로 줄에서 세로 정렬을 덮어쓴다.** `alignSelf: 'flex-start'`는 가로 줄에서 **위쪽**을 뜻해 부모의 `alignItems: center`를 이긴다 — 학원 카드의 `연결 끊기`가 위로 13px 붙어 있었다. 그래서 `Section`의 `action`과 `Row`의 `trailing`은 **감싸는 `View`를 하나 둔다**: 내용 높이인 그 안에서는 `alignSelf`가 아무 일도 하지 않고, 바깥에서는 가운데 정렬이 그대로 산다. 새로 슬롯을 만들 때도 같게 한다 — 호출부 20곳에서 `hug`을 빼는 것보다 슬롯 하나가 감싸는 쪽이 다시 틀릴 여지가 없다.
      - **인증 화면의 주 경로는 강조색을 칠한다.** `휴대폰 번호로 로그인`·`휴대폰 번호로 가입하기`가 `secondary`였는데, 크림 배경 위에서 면 색이 거의 같아 상자로만 보이고 버튼으로 읽히지 않았다. 이때 `leading` 아이콘 색도 `accentText`로 함께 바꾼다 — 호출부가 `colors.ink`로 고정해 두면 채운 배경 위에 어두운 글리프가 남는다.
    - **③ 그 밖의 버튼은 오른쪽 끝에 둔다.** 되돌아가기·건너뛰기처럼 곁다리인 것을 전폭으로 늘이면 주 행동과 같은 무게가 된다. 왼쪽에 몰면 본문 글줄과 붙어 어디까지가 읽을 것이고 어디부터가 누를 것인지 흐려진다.
    - **④ 한 대상에 속한 행동은 여기 두지 않는다.** 카드 한 줄에 딸린 것은 그 줄의 `trailing`으로(`학원 연결 끊기`는 학원 이름 옆이다), 섹션에 딸린 것은 `Section`의 `action`으로(`질문하고 메모하기`는 `오답노트` 제목 옆이다). 화면 아래로 내려오면 무엇에 대한 행동인지 사라진다.
    - **선택지가 여럿이면 버튼을 늘어놓지 않는다.** `Group` + `Row`(chevron)로 한 줄에 하나씩 고르게 한다. 예전 `오답노트 복습하기` · `별표 3개만` · `질문하고 메모하기` 세 버튼이 그 반례다 — `별표 3개만`은 **이름만으로 무엇을 하겠다는 것인지 알 수 없었다.** 지금은 `전체 복습하기` / `별표 친 것만 복습하기` / `{영역} 복습하기` 줄이고, 넓은 것부터 좁은 것 순이다. 영역 줄에서 `만`을 뺀 이유는 D-130에 있다 — `별표 친 것만`(상태)과 `문학만`(분류)이 같은 접미사로 다른 뜻이었고, `화법과 작문만 복습하기`는 조사가 붙어 읽히지 않았다.
    - **화살표(`arrow-right`)는 지금 하던 일을 끝내고 다음으로 넘어가는 행동에만 붙인다.** 방식을 고르거나 옆길로 새는 행동에는 붙이지 않는다 — `질문하고 메모하기`에 화살표가 붙어 있으면 이 화면이 끝나는 줄 안다. 목록 줄의 이동 표시는 chevron이고, 버튼 안에 chevron을 넣지 않는다.
    - `stretch` prop은 **`primary`가 아닌데 화면의 목적을 끝내는 드문 자리**에만 손으로 준다. 보통은 규칙 ②가 알아서 한다.
    - **행동 줄 안에서는 `hug`을 쓰지 않는다.** 가로 배치라 버튼이 이미 내용 폭이다.
    - **행동 줄 안에 `ghost`를 두지 않는다.** 테두리 없는 버튼이 줄 안에 섞이면 그것만 글자로 보인다. `ghost`는 목록 줄 안(R3)과 펼침·링크에만 남는다.
    - **확인 단계(`ConfirmStep`)만 규칙 ①에서 빠진다.** 그 자체가 고르는 단계라 `취소`가 결론 바로 옆에 있어야 한다. 그래서 `ActionBar`를 쓰지 않고 자기 줄을 갖는다 — 오른쪽 끝 정렬은 같다.
  - **행동은 네 종류뿐이고 종류마다 모양이 하나다.** 새 버튼을 놓을 때 어느 종류인지부터 정한다.

| 종류 | 모양 | 예 |
|---|---|---|
| **R1 화면 primary** | `primary`. 화면당 **최대 1개**, 그 화면의 목적을 끝내는 것만. 전폭이 허용되는 유일한 버튼 | `제출할게요` · `배정하기` · `등록할게요` |
| **R2 섹션 제목 옆** | `secondary` + `tone="accent"` + `size="sm"` + `hug`. **절대 primary가 아니다** | `N개 더 보기` · `학습 배정하기` · `문제 등록하기` |
| **R3 목록 안 반복 행동** | `ghost` + `leading` 아이콘 + `style={{ height: 44 }}` (D-084) | `제외하기` · `마감일 다시 정하기` · `되돌리기` |
| **R4 접힌 폼의 제출** | `hug`, 옆에 `ghost` 취소. 그 화면의 R1이 아니면 `secondary` + `tone="accent"` | `이름 바꾸기` · `다시 배정하기` |

  - 관리 화면(`/academy/classes/[id]`·`/admin/*`)은 R1이 **없는 것이 정상**이다 — 목적을 끝내는 행동이 없는 화면이다.
  - **테마 전환은 상단 줄 오른쪽 끝의 아이콘 하나다**(D-165). 로그인 전 화면(소개·로그인·가입)에만 둔다 — 로그인한 뒤에는 `내 정보`의 `AccountSettings`가 정석 자리다. 라벨을 붙이지 않는 이유는 지금 상태가 아이콘 모양으로 보이기 때문이고(해·달·모니터), 이름은 `accessibilityLabel`이 지킨다. 예전에는 `테마 · 라이트` 버튼이 약관 문구 **아래** 화면 맨 끝에 있었다.
  - **`ghost`는 링크와 펼침에만 쓴다.** 진짜 명령(`그대로 둘게요`·`질문하고 메모하기`)은 `secondary`다 — 배경도 테두리도 없으면 옆 캡션과 구분되지 않는다.
  - **누르면 버튼이 살짝 눌리고 `trailing` 화살표가 가는 쪽으로 밀린다**(누름 4px, 웹에서 올려놓으면 2px). `motion.duration.quick`. 불투명도만 바꾸면 눌렸는지 확신이 안 선다.
  - `tone="accent"`는 secondary·ghost의 라벨을 강조색으로 만든다. 화면의 primary와 무게가 겹치지 않게 보조 행동을 눈에 띄게 할 때만.
  - **안내 문구를 버튼 모양으로 만들지 않는다.** 버튼 라벨은 늘 '무엇을 하는지'만 말한다(`아직 3문항 남았어요`처럼 상태를 라벨에 넣으면 버튼인지 안내인지 알 수 없다).
  - 아이콘은 `leading`(앞)·`trailing`(뒤) 슬롯에 둔다. **다음 단계로 넘어가는 행동은 `trailing`에 `arrow-right`**(`제출할게요` → · `문제 담으러 가기` →), 페이지 이동은 `chevron-left`·`chevron-right`. 옆길로 새는 행동에는 붙이지 않는다 — `질문하고 메모하기`가 그 예다(§17). 아이콘을 두면 읽히는 이름이 흐려지므로 `accessibilityLabel`로 이름을 고정한다.
- **일시적 알림**(`Toast`): 되돌릴 수 있는 행동을 한 줄로 알린다. 화면 아래에서 페이드+살짝 올라오며 나타나고 약 2.4초 뒤 스스로 사라진다(`useNativeDriver: false`, 새 의존성 없음). `pointerEvents="none"`이라 떠 있는 동안에도 화면을 계속 쓸 수 있다. **되돌리기가 필요한 삭제는 `action`을 붙여 알림 안에서 되돌린다**(D-091).
  - `show(문구, kind, { label: '되돌리기', onPress })`. 행동이 있으면 알림이 `box-none`으로 눌리게 되고 **2.4초 → 6초**로 길어진다(8초를 넘기면 `e2e/_toast.ts`의 대기가 깨진다). 문구 쪽은 `none`으로 다시 막아 알약 전체가 화면을 덮지 않게 한다.
  - 행동 버튼은 `Button`이 아니라 맨 `Pressable`이다 — 알림 배경이 `ink`인데 ghost 라벨색도 `ink`라 글자가 보이지 않는다. 누름 영역 44px, testID는 `toast-action`.
  - 행동이 있을 때만 아래 여백을 `spacing.xxl` 더한다 — 태블릿의 떠 있는 탭 알약과 사이가 7px뿐이다.
  - **`ToastProvider`가 한곳에서 그린다**(`src/features/toast.tsx`). 화면은 `useToast().show(문구, kind)`만 부른다. 화면마다 상태를 들고 `Screen` 밖에 두려면 화면마다 감싸는 구조를 바꿔야 했다.
  - **배경은 `ink`로 진하게 두고 강조색은 아이콘에만 쓴다**(`check-circle` / `minus-circle`, `accent`). 배경을 `accent`로 바꾸면 다크에서 글자색이 뒤집히고(`accentText`가 거의 검정) 대비가 16.6:1 → 4.6:1로 떨어진다.
  - **아이콘은 문구의 형제로 둔다.** `testID`가 붙은 요소 안에 넣으면 Feather 글리프가 그 요소의 텍스트에 섞여 문구를 그대로 확인하는 테스트가 깨진다. 장식이므로 이름을 주지 않는다 — 뜻은 문구가 다 말한다.
  - 문구는 담기 종류마다 구분한다: 오답노트 문항은 `문항을 오답노트에 담았어요` / `오답노트에서 뺐어요`, 학습은 `학습을 담아 뒀어요` / `담아 둔 학습에서 뺐어요`(§17의 두 담기 구분과 짝).
  - **동사는 토글 이름과 같아야 한다.** 토글이 `담아 둔 학습에서 빼기`인데 알림이 `취소했어요`라고 하면, 스크린리더 사용자는 "빼기"를 누르고 "취소"를 듣는다. 넣기는 `담아 두기`, 빼기는 `빼기` 한 벌로 쓴다.
- **모션은 `src/theme/motion.ts`의 토큰만 쓴다.** 시간 셋(`quick 180` · `basic 260` · `slow 620`)과 곡선 둘(`standard` · `decelerate`)뿐이고 화면에서 ms를 직접 쓰지 않는다. 구현은 `Animated` + `useNativeDriver: false`다. 새 의존성은 넣지 않는 것이 기본이고(D-038), 유일한 예외가 모션 애셋 재생부다(D-121 — 지금은 호출부가 없어 번들에 들어가지도 않는다).
  - **높이·레이아웃은 애니메이션하지 않는다.** `LayoutAnimation`은 react-native-web에서 아무 일도 하지 않아(`UIManager.configureNextLayoutAnimation`이 완료 콜백만 부른다) iOS에서만 움직이게 된다. `opacity`·`transform`만 쓴다.
  - 쓰는 곳은 `src/theme/useMotion.ts`의 훅 둘이다: `useReplayFade`(자리는 그대로인데 내용만 갈릴 때 — 문항 페이지, 펼친 내용), `useAnimatedTo`(켜짐/꺼짐 — 별표·담기 토글의 아주 짧은 확대).
  - 정적 화면으로 충분하면 넣지 않는다. 화면 전환 애니메이션은 별도 항목이다(Q-010).
  - **모션 줄이기를 켜면 움직이지 않는다**(D-119). `src/theme/useReduceMotion.ts` 하나가 웹·네이티브를 모두 덮는다 — react-native-web이 `AccessibilityInfo.isReduceMotionEnabled()`를 `matchMedia('(prefers-reduced-motion: reduce)')`로 구현해 두어 플랫폼 분기가 필요 없다. **느리게 만들지 말고 아예 하지 않는다**: 목표값으로 즉시 가거나 정적인 것을 그리고, 무거운 것은 불러오지도 않는다. 지키는 곳은 `useMotion`의 훅 둘 · `Button` · `IconButton` · `Toast` · `Reveal` · `PendingDots`다.
- **기다리는 동안에만 대기 표시를 둔다**(D-120). 이 앱에서 실제로 몇 초가 걸리는 것은 **AI 호출뿐**이고, 나머지는 전부 즉시 끝나 표시할 것이 없다. 토스 그래픽 가이드도 같은 말을 한다 — *"기다릴 필요가 없는데 로딩 애니메이션을 사용하는 경우 사용자가 상황을 오해할 수 있어요."*
  - 쓰는 곳은 넷이다: `app/student/ask.tsx` · `notebook.tsx`(답 대기·정리 대기) · `review.tsx` · `src/components/ChildReport.tsx`(주간 요약).
  - **상태는 글자가 말한다.** `답을 쓰고 있어요` 같은 문장이 반드시 함께 있어야 하고, 모션은 그 옆에서 "멈춘 게 아니다"만 거든다. 그래서 모션에는 `aria-hidden`을 준다 — 같은 말이 두 번 읽히면 오히려 방해가 된다.
  - **완료·빈 상태·오류에는 넣지 않는다.** 제출 완료는 `ScoreCard`와 `다 풀었어요.`가, 빈 상태는 `EmptyState`가, 오류는 인라인 `danger` 캡션이 이미 조용한 확인을 하고 있다(§9의 `폭죽·큰 배지 금지`가 그 자리를 정확히 겨냥한다).
  - 형태는 `MotionAsset` 하나다. 모션을 줄이거나 재생이 실패하면 `PendingDots`(의존성 0, `opacity`만)로 떨어지므로 **화면은 애셋도 색도 몰라도 된다**. 크기는 `motion.asset` 토큰이 정하고(`inline 12` = 글자 옆 높이, `sm 20` = 아이콘 자리) **폭은 애셋이 선언한 비율에서 나온다** — 가로로 긴 애니메이션을 정사각에 넣으면 눌려 작아진다.
- **모션 애셋은 직접 만든다**(D-121). `assets/motion/build.ts`가 Lottie를 코드로 만든다 — 순수 벡터, 강조색 하나, 이미지 임베드·매트·마스크 없음. 남의 애셋을 쓰지 않는 이유와 굳이 넣을 때의 절차는 `assets/motion/README.md`에 있다.
  - 지금 있는 것은 둘이다: `pending`(점 셋이 숨 쉬듯 작아졌다 돌아온다, 반복) · `check`(선이 스스로 그려지는 완료 체크, **한 번만**). 반복하면 완료가 아니라 진행 중으로 읽힌다.
  - **`check`가 Lottie를 쓸 값이 있는 자리다** — 선이 그려지는 것(trim path)은 코드로 하기 어렵다. 토스도 토스트 완료 아이콘을 같은 방식으로 만든다. 반대로 점 셋은 코드로도 되므로, 대체물이 그 역할을 그대로 한다.
  - 값은 공짜가 아니다: 재생부를 쓰면 번들이 **+61 kB gzip** 는다. 애니메이션을 더할 때마다가 아니라 **재생부를 켤 때 한 번** 드는 비용이라, 새 애셋은 그 위에서 거의 공짜다.
- **하나를 고르는 자리는 전부 `SegmentedControl`이다**(D-077). 목록 필터(역할·이용권·영역·반), 보기 방식 전환(5문항씩/한 문항씩, 틀린 문항/전체), 입력 폼의 선택(문제 등록의 유형·학년·영역·세부 유형·정답, §18-3), 대상 전환(자녀 고르기)이 모두 같은 컨트롤이다. 칩·라디오를 손으로 그리지 않는다 — 선택 표현이 화면마다 갈린다.
  - **역할은 `radio`, 상태는 `aria-checked`, 묶음은 `radiogroup`이다**(D-166). 하나만 고르는 컨트롤이라
    그것이 맞는 역할이고, 그래야 "라디오 버튼, 선택됨, 3개 중 1번째"로 읽힌다. 예전에는 `button` +
    `aria-selected`였는데 그 조합은 보조기술이 무시한다 — 밝은 면으로 떠오른 칸이 그냥 버튼 셋이었다.
    `aria-pressed`도 쓰지 않는다: 웹에서는 맞지만 react-native의 `Pressable`이 모르는 속성이라
    네이티브에서 상태가 사라진다.
  - **테스트는 칸을 이름으로 고른다.** 순서(`getByRole('radio').first()`)로 세면 같은 화면에 라디오
    묶음이 둘 이상일 때 조용히 다른 것을 누른다 — 풀이 화면이 실제로 그랬다(보기 방식 토글 + 보기).
  - 모양: `offset` 트랙 안에서 고른 칸만 `surface` 면으로 떠오르고 글자는 `accent` + semibold. 비선택은 `inkSecondary` + medium, 테두리는 없다. 치수는 `src/theme/tokens.ts`의 `control` 하나에서 온다 — 화면에서 다시 정하지 않는다.
  - **옵션이 한 줄을 넘으면 트랙 안에서 줄바꿈한다.** 가로로 잘라 숨기지 않는다 — 모바일에서 오른쪽 옵션이 보이지 않으면 있는 줄도 모르고 지나친다. 트랙 radius가 `pill`이 아니라 `card`인 이유다(한 줄이면 알약으로 잘리고, 두 줄이면 둥근 사각형이 된다).
  - **선택지가 너무 많으면 컨트롤이 아니라 목록 설계를 고친다.** 반·과제처럼 개수가 가변인 목록은 6개를 넘으면 `Field` 검색으로 바꾼다(`app/academy/classes/students.tsx`의 `SEGMENT_MAX`).
- **목록 행의 행동**: `Row`·`LearningRow`의 `trailing`은 행을 누르는 영역 **밖**에 놓인다. 안에 넣으면 행동을 눌러도 행 배경이 함께 번쩍인다.
  - **`trailing`이 있으면 chevron을 두지 않는다.** chevron은 누름 영역 기준 `absolute right`인데 `trailing`이 붙으면 그 영역의 오른쪽 경계가 밀려 chevron이 행동 버튼 왼쪽으로 끌려 들어온다(측정: `>` 79px / `+` 16px). 행은 여전히 누를 수 있고 오른쪽에는 행동 하나만 남는다. `LearningRow`는 자동으로 처리하고, `Row`는 `showChevron`을 `trailing`과 함께 주지 않는다.
- **목록 행의 마감도 오늘 기준으로 말한다**(D-142). `LearningRow`는 `dueLabel` 하나를 써서 학생 홈 히어로·학부모 리포트·학원 성과 분석과 **같은 문장**(`마감이 지났어요`·`오늘까지`·`내일까지`·`N월 N일까지`)을 쓴다. 날짜만 적으면(`8월 6일 마감`) 지난 마감이 여유 있게 읽힌다.
  - **글자가 먼저 바뀌고 색은 그다음이다**(§11). 지난 마감은 `마감이 지났어요`로 문장이 바뀐 뒤에만 `danger`가 얹히고, 색을 못 보는 사람에게도 뜻이 그대로 남는다.
  - 히어로는 지난 마감을 **한 줄로 분리**하고(§14의 1번) 목록 행은 메타 줄 안에 둔다 — 행은 촘촘해야 하고, 히어로는 제목 아래에 자리가 있다.
  - **이미 낸 학습에는 오늘 기준 문장을 쓰지 않는다.** 낸 과제에게 마감일은 지난 일이라 날짜(`8월 6일 마감`)와 정답률만 남긴다. 히어로에는 남은 학습만 오므로 이 경우가 없지만 목록에는 완료한 과제가 함께 서 있고, 그 줄을 빨갛게 칠하면 할 일이 남은 것처럼 읽힌다(§13의 `의미 없는 배지`와 같은 갈래).
  - **학습 상세도 `dueLabel`을 쓴다**(`app/student/[id].tsx`). D-142는 이 줄을 `formatDate`로 정했지만 그때 없애려던 결함은 **ISO 원문 노출**(`2026-08-20까지`)이었다 — 날짜만 남은 뒤에도 어제가 마감인 과제가 `마감 8월 17일까지`로 읽혀, 같은 사실을 홈 히어로와 목록 행이 `마감이 지났어요`로 말하는 것과 어긋났다. **지난 마감은 `Row` 밖 한 줄로 분리한다**(히어로와 같은 한 벌 — 글자가 먼저 바뀌고 색은 그다음). `meta`는 가장 흐린 글자라 지난 마감을 담을 자리가 아니다(위 `meta`에 값을 두지 않는다).
  - ISO 원문(`2026-08-20`)을 화면에 내보내는 자리를 만들지 않는다. 날짜만 필요한 곳은 `formatDate`를 쓴다(학습 상세에서 **이미 낸** 과제의 `마감` 줄).
  - **지난 마감 뒤에는 지금 무엇을 할 수 있는지 말한다.** 서버는 마감을 검사하지 않으므로(`rpc_submit_attempt`의 가드는 배정 여부·콘텐츠 일치·문항 존재만 본다) 마감이 지나도 낼 수 있다. 그 사실을 말하는 곳이 없으면 빨간 글자만 보고 손을 놓는다. 문장은 하나로 고정한다 — `마감이 지나도 낼 수 있어요. 선생님에게는 늦게 낸 것으로 보여요.` 두는 자리는 **학습 상세의 마감 줄 아래와 풀이 화면 상단**이고, 학원 과제에만 붙인다(개인 학습에는 마감도 볼 사람도 없다).
- **한 화면 목록은 5줄까지.** 그 이상은 `N개 더 보기`(같은 화면에서 펼치기) 또는 전체 목록 화면으로 넘긴다(`app/academy/classes.tsx`의 `showAll` 패턴).
- **대화 입력창**(`AskField`): 텍스트 영역과 전송 버튼을 위아래 두 층으로 나눈 컴포저다(ChatGPT·Perplexity 방식). 여러 줄을 쓸 수 있고 줄이 늘면 `maxHeight`(기본 200)까지 자란 뒤 안에서 스크롤한다. 화면 하단에 고정해 쓰는 곳은 낮춰 잡는다(대화 화면 140) — 키보드까지 뜨면 대화가 보일 자리가 없어진다. **Enter 보내기 / Shift+Enter 줄바꿈**은 react-native-web 기본 동작(`multiline` + `blurOnSubmit` + `onSubmitEditing`)을 쓴다 — 한글 조합 중에는 보내지 않는다. `Group` 안에 넣을 때는 `flat` + `dividerInset={0}`(구분선은 카드가 긋는다).
  - 높이는 **웹과 네이티브가 다른 길로 잰다.** 웹에서 `onContentSizeChange`만 쓰면 줄어드는 방향을 감지할 수 없다 — textarea에 명시적 height가 걸려 있으면 `scrollHeight`가 그 아래로 내려가지 않고, react-native-web이 같은 값을 캐시로 걸러 콜백을 아예 부르지 않는다. 그래서 웹은 값이 바뀔 때 height를 0으로 풀어 `scrollHeight`를 직접 재고 되돌린다. 네이티브는 `contentSize`가 실제로 줄어들어 그 값을 그대로 쓴다.
- **입력**(`Field`): 라벨(캡션) 위, 높이 44~48, border `borderStrong`, focus 시 border=accent. placeholder=inkTertiary. 항상 `accessibilityLabel`.
- **카드/그룹**(`Group`+`Row`): 얇은 테두리 컨테이너에 행을 담고 행 사이 hairline. 카드를 장식으로 반복하지 않는다. 목록=Group, 지표=metric 타일.
- **네비게이션**(`RoleShell`): 데스크톱 좌측 사이드바(브랜드→메뉴→계정/테마/로그아웃), 모바일 상단바(브랜드)+**하단 플로팅 라운드 내비**(Toss식 pill: 화면 아래에서 살짝 떠 있고 콘텐츠가 그 아래로 스크롤). 선택 항목=accent 아이콘/텍스트 + `accentSoft` pill 하이라이트, 나머지=inkTertiary. 학생은 아이콘만, 학부모·학원은 아이콘+라벨.
- **행(Row)**: 최소 높이 48, 좌 타이틀/서브타이틀, 우 meta/chevron. 이동 가능한 행만 chevron.
  - **`meta`에 값을 두지 않는다.** `meta`는 `inkTertiary`(대비 3.23:1, AA 미달)라 화면에서 가장 흐린 글자다. 판단에 쓰는 값(단가·개수·비율)은 `trailing`의 `AppText variant="label"`로 두고, `meta`에는 값이 아닌 **분류**만 남긴다(`메모리`·`합성`·`추정`·`테스트 집계`·출처). 학부모 리포트(§19)와 운영 기록이 같은 규칙을 쓴다.
- **결과가 0건인 필터 칸은 렌더하지 않는다.** 누르면 빈 목록만 나오는 죽은 버튼이 된다. `전체` 칸은 항상 두고, 나머지는 `count > 0`일 때만 만든다(운영 기록의 감사 로그 분류, 학원 문제의 출처 필터).
  - **그래서 남은 선택지가 `전체` 하나뿐이면 `SegmentedControl` 자체를 렌더하지 않는다.** 이미 선택된 칸 하나만 놓인 선택 컨트롤은 고를 것이 없다 — 개수는 섹션 제목이 이미 말한다(D-075). 0개인 **목록 칸**을 막는 방법은 다르다 — 그쪽은 자리를 남기고 `아직 준비 중이에요`로 밝힌다(D-042).

## 9. Loading, empty, error, success states

- **로딩**: 초기 폰트/로드는 빈 게이트. 데이터 로딩은 텍스트 스켈레톤(회색 바), 스피너 남발 금지.
- **빈 상태**: Group 안 한 줄 안내 + 가능하면 다음 행동. 예: "아직 푼 학습이 없어요. 첫 학습을 시작해볼까요?"
- **읽는 중에는 없다고 말하지 않는다**(D-133). 개수(`남은 학습 3개`)나 없음(`아직 …이 없어요`)을 말하는 자리는 provider의 `loading`이 내려간 뒤에 그린다. 조회가 끝나기 전에는 목록이 비어 있어서 빈 상태 문장이 **사실이 아닌 채로** 먼저 보인다.
  - **화면 전체를 막지 않는다.** 개수를 말하는 자리만 기다리고, 그 자리에는 `학습을 불러오고 있어요.` 한 줄(`AppText variant="caption" tone="secondary"`)만 둔다. 문장은 세 화면이 같다(`student/pick` · `student/index` · `student/learn`).
  - **빈 카드를 남기지 않는다.** 카드 껍데기만 두고 제목을 비우면 그 빈 칸이 곧 "할 일이 없다"는 뜻으로 읽힌다.
  - **게이트는 첫 조회에만 건다**(D-163 · D-168). provider의 `loading`은 `reading || !loaded`라 **재조회마다** 참이 되고, 쓰기가 실패하면 화면을 서버 값으로 되돌리려고 `reload()`가 돈다 — `loading`으로 목록을 감추면 실패 한 번이 손에 있는 목록을 통째로 지운다. 그래서 `loaded`(= 첫 조회가 끝났는지)를 보고 `firstLoad = !progressLoaded || !contentLoaded`로 게이트를 건다. 첫 조회가 **실패해도** `loaded`는 참이 되므로(두 provider가 성공·실패를 함께 끝으로 본다) 실패 줄이 정상적으로 나오고 화면이 `불러오고 있어요`에서 멈추지 않는다. 지금 이 규칙을 쓰는 화면은 `student/index` · `student/review`이고 `student/learn` · `pick` · `result/[id]`는 아직 `loading`을 본다(마스터 플랜 A-126).
  - **불러오는 중이라는 말은 모든 갈래에 있어야 한다.** 히어로의 한 가지에만 두면, 다른 갈래를 타는 계정에서는 목록이 문장 없이 빈다(D-168 실측).
- **실패는 빈 목록과 다르게 말한다**(D-136). 조회가 실패하면 `loading`이 내려가므로 로딩 게이트가 덮지 못한다 — 그래서 provider가 `error`(사람이 읽을 문장 또는 `null`)를 함께 내보내고, 화면은 **읽는 중 · 실패 · 빈 목록**을 셋으로 가른다.
  - 실패 자리는 인라인 `danger` 캡션 + **다시 시도할 행동 하나**다(`다시 불러오기` → provider의 `reload()`). 문장은 서버가 준 것을 쓴다(`src/lib/supabase.ts`의 `errorMessage`). 기준은 `student/index` · `student/learn` · `student/pick` · `student/result/[id]`이고, 같은 갈래가 `academy/manage`의 `초대를 불러오지 못했어요`다.
  - **한 화면에 실패 면은 하나다.** 여러 자리가 같은 조회에 매달려 있으면 자리마다 빨간 줄을 두지 말고 맨 위에 한 줄만 둔다 — 한 번의 실패가 네 번으로 읽힌다.
  - **다시 읽는 중에는 실패 문장을 감춘다.** 실패와 `학습을 불러오고 있어요.`가 함께 서면 지금 무슨 일이 일어나는지 알 수 없다.
  - **실패했을 때 개수를 세지 않는다.** 못 읽은 목록으로 센 숫자는 읽는 중에 센 숫자와 똑같이 거짓이다. 반대로 **이미 읽어 둔 값은 지우지 않는다** — 가진 것은 여전히 사실이다.
- **조회 실패는 `LoadFailed` 하나다**(D-169 · `src/components/LoadFailed.tsx`). 학생 화면 넷(홈·학습·고르기·결과)이 글자까지 같은 블록을 갖고 있었고, 그래서 결함 하나를 고치는 데 네 곳을 손대야 했다. 화면이 정하는 것은 `무엇을` 불러오지 못했는지 하나다.
  - **다시 시도가 도는 동안 버튼을 언마운트하지 않는다.** 웹에서 포커스된 요소가 사라지면 포커스가 `<body>`로 떨어지고, 새로 마운트되는 영역은 대부분의 보조기술이 읽지 않는다(§11). 같은 버튼의 **라벨만** `다시 불러오고 있어요`로 갈아 진행을 말한다(`/staff`의 `들어가는 중이에요`와 같은 방법).
  - **게이트는 첫 조회에만 건다**(D-168·D-169). provider의 `loading`은 `reading || !loaded`라 재조회마다 참이 되므로, 그것으로 목록을 감추면 쓰기 실패 뒤 손에 있는 값이 사라진다. `loaded`를 본다.
- **오류**: 인라인 캡션 `danger` + 사람 문장. 예: "아이디 또는 비밀번호를 확인해 주세요." 파괴적 행동은 확인 단계.
- **성공/완료**: 조용한 확인. 정답=success 점+텍스트, 완료=진행률 반영. 폭죽·큰 배지 금지.

## 10. Responsive behavior

- 분기점: `mobile 0 · tablet 720 · desktop 1024`.
- 데스크톱: 사이드바 + 본문(폭 제한). 태블릿/모바일: 하단 탭 + 상단바, 본문 폭 100%.
- 터치 타깃 최소 44px(`touch.min`). 표·긴 콘텐츠는 가로 스크롤 컨테이너.
- **`Table`의 `priority`는 분기점 등급이 아니라 폭이 모자랄 때 포기하는 순서다.** 표는 지금 **컬럼 폭**(`useColumn`, 창 폭이 아니다)에서 들어가는 만큼 보여 주고, 안 들어갈 때만 3 → 2 순으로 포기한다.
  - **네 검사 중 하나에 걸리면 `1`이다 — 어떤 폭에서도 포기하지 않는다.** ①표 위 캡션이나 섹션 제목이 그 열을 언급하는가 ②기본 정렬 기준인가 ③없으면 이 행이 무엇에 대한 것인지 모르는가 ④그 값을 보고 다음에 할 일이 갈리는가.
  - `1`인 열이 절반을 넘으면 표가 아니라 화면 설계 문제다 — 열을 줄이거나 화면을 나눈다.
  - **컬럼이 560보다 좁으면 표 모양을 포기하고 쌓는다**(`narrow="stack"`, 기본). `제목 줄 + 라벨/값` 레코드라 **열이 하나도 사라지지 않고** 가로로 밀 것도 없다. 정렬은 위쪽 줄로 옮긴다.
    - `narrow="fold"`는 `이름/값` 쌍으로 줄어도 표의 약속이 남는 지표 표에만.
    - `narrow="scroll"`은 **행과 열이 모두 같은 종류의 축인 행렬에만**(코호트 = 가입주 × 경과주). 쌓으면 12행 × 9열이 108줄이 된다.
  - 접기는 *덜 중요한 열*을 지우는 도구다. 이 앱의 표들은 접으면 *비교 대상*이 지워졌다 — 반별 현황은 담당·학생·배정·정답률이, 선생님 마감 표는 반 이름이 사라졌다. 그래서 쌓기가 기본이다.
  - **글자 한 줄인 링크는 `tap.textLine`으로 44를 만든다**(`src/theme/styles.ts`). 커진 만큼 음수 마진으로 되돌려 줄 높이는 그대로다 — `IconButton`의 `inset`이 아이콘에 하는 일과 같다. 이 손기술이 다섯 곳에 복사돼 값이 네 가지로 갈려 있었다(D-166). 글자가 캡션 한 줄(약 20px)보다 크면 그 자리에서 자기 높이로 계산한다(`Brand`의 워드마크).
  - **`SegmentedControl`은 예외로 36px이다**(`touch.dense`). 필터를 알약 44개로 늘어놓으면 화면이 무거워진다는 판단이고(D-077), 칸이 서로 붙어 있어 오조준해도 이웃 칸에 떨어진다. WCAG 2.5.8(AA) 24는 넘고 2.5.5(AAA) 44에는 미달이다 — **이 예외를 다른 컨트롤로 넓히지 않는다.**
    - 예전 문서는 `약 34px`이라고 적었지만 실제 높이는 **31.25px**이었다. `fontSize`만 13으로 바꾸고 `AppText body`의 줄높이 23.25를 상속한 **사고**였다 — 아무도 적어 두지 않은 값에 치수가 매달려 있었고, 누가 줄높이를 "고치면" 28.15로 조용히 줄었다. 지금은 `control.labelLineHeight`로 명시한다.
  - **좌상단 뒤로가기**(`BackLink`)도 44px을 지킨다. 캡션 한 줄(약 20)에 위아래 `spacing.md`를 더해 맞추고, 커진 만큼 위아래 음수 마진(`-spacing.sm` / `-spacing.lg`)으로 되돌려 이 컴포넌트를 쓰는 화면들의 여백은 그대로 둔다. 단계형 화면에서는 이것이 유일한 이탈 장치다(D-039).
- 3뷰포트(desktop/tablet/mobile) Playwright로 검증.

### 모바일 전용 조정 (390)

- **`Screen`의 좌우 여백과 섹션 간격을 한 단계 줄인다**(`spacing.xl` → `spacing.lg`). 폭이 342px뿐인데 24를 그대로 쓰면 본문이 눌리고 화면이 성기게 보인다. 태블릿·데스크톱은 그대로 둔다.
- **하단 내비는 화면에 붙이고 위 모서리만 둥글게** 한다(`radius.card`, `borderTopWidth`만). 떠 있는 알약은 좌우·아래 여백까지 먹어 좁은 화면에서 본문을 더 밀어낸다. 아이콘은 19, 세로 패딩은 `spacing.xs`. **태블릿은 떠 있는 알약을 유지한다.**

## 11. Accessibility rules

- 모든 입력에 `accessibilityLabel`. 버튼/링크/라디오/탭은 정확한 role과 이름.
- **`accessibilityState`·`accessibilityValue`를 쓰지 않는다. ESLint가 막는다.**
  - react-native-web 0.21.2는 두 객체를 DOM으로 옮기지 않는다(`dist/modules/forwardedProps/index.js`의 허용 목록에 두 이름이 없다). 선언해도 조용히 사라져서, 화면에서는 상태가 바뀌는데 스크린리더는 계속 같은 값을 읽는다. 실측으로 **선언 14곳이 전부 무효**였고 그중에 모든 문항의 답 선택 상태가 있었다.
  - 대신 `aria-selected`·`aria-checked`·`aria-expanded`·`aria-valuenow`를 **직접 준다.** 네이티브는 `react-native/Libraries/Components/View/View.js`와 `Pressable.js`가 이 값을 보고 `accessibilityState`를 다시 만들어 준다 — 한쪽만 써도 두 플랫폼이 모두 맞다.
  - 현재 페이지에는 `aria-current="page"`를 함께 준다.
  - **`Button`처럼 감싸는 컴포넌트는 `aria-*`를 직접 받아 넘겨야 한다.** 넘기지 않으면 조용히 사라진다(`aria-expanded`가 그래서 한 번 빠졌다).
- **포커스 링은 한곳에서 정한다**(`src/theme/palette.ts`의 `FOCUS_CSS`, `ThemeProvider`가 주입). `:focus-visible`에 `outline: 2px solid var(--sc-accent)` — 마우스 클릭에는 뜨지 않고, CSS 변수라 테마 전환이 그대로 먹는다. 컴포넌트마다 그리지 않는다.
  - 예외는 `AskField` 하나다. 자기 포커스 표시(테두리·면 변화)가 있고 사각 링이 `radius.xl`과 싸운다.
- **완료를 알리는 알림은 스크린리더에도 닿아야 한다.** `ToastProvider`가 항상 마운트된 `LiveRegion`을 들고 문구만 바꾼다(`role="status"`, 행동이 있으면 `alert`). 영역이 내용과 같이 나타나면 대부분의 보조기술이 읽지 않아서, `Toast`가 아니라 provider가 들고 있다.
- 색만으로 의미 전달 금지 — 정답/오답·출처는 항상 텍스트 병행.
- 본문 대비 충분히. 포커스 가시성 유지. 긴 글은 상대 단위·줄간격 확보.

## 12. Explicit do and don't

**Do**
- 정보 계층 먼저, 스타일 그다음.
- 목록=Group+hairline, 지표=metric 타일, 글=기사처럼.
- 짧고 사람다운 한국어.

**Don't**
- 보라/파랑 AI 그라데이션.
- 의미 없이 반복되는 둥근 카드.
- 과도한 그림자·Glassmorphism.
- 모든 요소 중앙 정렬.
- "큰 문구 + 카드 3개"로 끝나는 SaaS 데모 구조.
- 장식용 아이콘 남발.
- 모든 버튼·카드가 같은 시각 무게.
- **보조 버튼을 전폭으로 늘리기.** primary 하나만 전폭이고 나머지는 `hug`이다(§8).
- **글자만 있는 `ghost` 버튼.** 배경도 테두리도 없으면 아이콘 하나는 있어야 눌리는 곳으로 읽힌다(§8).

## 13. AI-generated UI anti-patterns (피할 것)

- **좌측 컬러 보더 + 배경 틴트 callout 박스**(해설·노트·인용 등에서 왼쪽만 튀어나온 색 바 + 다른 배경). 대표적 AI 티 → 라벨 + 기사형 문단으로 대체.
- 카드마다 컬러 왼쪽 액센트 바.
- 채운 컬러 pill 배지(정답/오답).
- 거대한 컬러 숫자(정답률 44px 등).
- 파랑·보라 강조 남발.
- 히어로 큰 문구 + 동일 무게 카드 3개.
- 과한 라운드(16px+) + 큰 그림자 조합.
- 중앙 정렬된 텅 빈 화면.

## 14. Student home-specific hierarchy

로그인 후 **3초 안에** 다음이 보여야 한다.

**화면은 세 덩어리다**(D-167). 덩어리가 바뀌는 자리만 간격을 벌린다 — `Screen`의 컬럼 간격은 가로
여백과 같은 값이라 그대로 두면 모든 경계가 같은 무게가 되고 세 덩어리가 한 줄기로 읽힌다.
**더하는 값은 폭마다 다르다**(D-168): 그 컬럼 간격이 모바일 `lg`(16) · 그 위 `xl`(24)이므로 경계는
§5의 다음 토큰까지 벌린다 — 모바일 24(`xl - lg`를 더한다) · 태블릿·데스크톱 36(`xxl - xl`).
한 값을 고정하면 한쪽 폭에서는 다른 경계와 같은 값이 되어 덩어리가 사라진다(모바일에만 8을 더해
820·1280에서 24 + 0이었다).

| 덩어리 | 무엇 | 아래 번호 |
|---|---|---|
| **확인할 것** | 조용한 한 줄들 — 칭찬 · 새 과제 · 조회 실패 | 0 |
| **할 일** | 오늘의 학습 → 진행 상황 → 남은 학원 과제 → 담아 둔 학습 | 1 · 2 · 3 · 4 |
| **막혔을 때** | Scody AI 입력창 | 5 |

**한 사실은 한 자리에서만 말한다.** 이 화면은 그 규칙을 세 자리에서 어기고 있었다 — 남은 과제가
하나인 학생에게 그 하나를 **네 번** 말했고(히어로 · 진행 상황 · 섹션 제목 · `위에 있는 과제 하나가
남았어요.`), 같은 목적지로 가는 버튼이 이름만 다르게 둘이었고, 처음 온 학생에게 `문제 담으러 가기`가
두 개였다. 새 블록을 놓기 전에 **위에서 이미 말한 사실인지** 확인한다.

시선 순서:

0. **확인할 것** — 아이콘 하나 + 한 줄. 카드로 만들지 않는다(오늘 할 일보다 무거워진다).
   - **칭찬**(D-051): 학부모가 보낸 것. 확인 버튼(44px)이 있고 확인하면 사라져 화면에 영구히 박히지 않는다.
     미확인이 여럿이면 한 줄만 그리고 나머지를 `외 N개`로 접는데, **확인 한 번은 그 줄 하나만
     닫는다**(D-168) — 접힌 것까지 함께 닫으면 학생은 하나만 읽고 나머지를 잃는다(지난 칭찬을 다시
     볼 화면은 앱에 없다). 누를 때마다 다음 칭찬이 이어서 나온다.
   - **아직 시작하지 않은 과제**(D-167 · 문구는 D-169): 아직 손대지 않았고 마감이 남은 학원 과제 수. **문구가 조건과 같아야 한다** — `새 과제가 배정되었어요`는 사건이고 조건은 지속 상태라, 3주 전에 받아 놓고 열지 않은 과제가 매일 `새 과제`로 인사했다. **섹션 안에 두지 않는다** —
     히어로가 대신할 수 없는 *상태 변화*이기 때문이다(어제 `마감이 지났어요`였던 것이 다시 열렸다는
     사실). 예전에는 4번 섹션 안에 있어서, 새 과제가 히어로에 올라가면 목록이 비어 **섹션째로
     사라졌다.** 마감이 지난 것은 세지 않는다 — 이미 늦은 일을 '새로 왔다'고 알리면 재촉이 된다.
   - **조회 실패**: 인라인 `danger` 캡션 + `다시 불러오기`. 면을 하나만 둔다 — 아래 네 블록이 모두 이
     조회에 매달려 있어서 자리마다 빨간 줄을 두면 한 번의 실패가 네 번으로 읽힌다.

1. **오늘의 학습(주인공)** — 가장 큰 히어로. 다음에 풀 학습 1개 + 그 학습을 여는 버튼 하나(화면의 유일한 primary). 마감은 오늘 기준으로 말한다(`마감이 지났어요`·`오늘까지`·`내일까지`·`N월 N일까지`). 지난 마감은 다른 메타에 이어 붙이지 말고 한 줄로 분리하고, 텍스트가 먼저 바뀐 뒤에만 `danger` 색을 얹는다.
   - **히어로 제목이 이 화면의 h1이다**(D-168). 세 가지 제목(학습 이름 · `아직 시작한 학습이 없어요` ·
     `오늘 할 일을 다 끝냈어요`) 모두 `AppText variant="title" headingLevel={1}`이다 — 홈은 `Screen`에
     `title`을 주지 않고 아래 섹션 둘은 조건부라, 그러지 않으면 제목이 0개인 상태가 생긴다(§4).
     크기·무게를 손으로 다시 적지 않는다: 줄간격 1.2 · 자간 -0.4로 쌓여 있던 것이 §4의 하한을
     벗어나 390에서 두 줄 제목을 뭉치게 했다.
   - **후보는 학생이 약속한 일뿐이다**: 담아 둔 학습 → 남은 학원 과제. 2번 진행 상황의 분모와 **같은 집합**이다(D-140). 공개 카탈로그에서 아무도 고르지 않은 세트를 `오늘의 학습`으로 올리지 않는다 — 학생이 고른 적도, 누가 시킨 적도 없는 항목이다. 새로 고르는 일은 아래 캡션과 5번 빈 상태가 맡는다.
   - **순서를 정하는 것은 출처가 아니라 마감이다**(D-170). 그 집합을 `byDue`로 정렬해 첫 줄을 올린다 — 마감이 있는 것 먼저, 그 안에서 마감이 이른 순, 마감이 없는 것끼리는 담은 순(정렬이 안정적이라 입력 순서가 남는다). 개인 학습에는 마감이 없으므로 급한 학원 과제가 담아 둔 학습보다 먼저 온다. 담아 둔 학습을 무조건 먼저 올리면 마감이 지난 과제가 히어로 아래 목록 한 줄과 `sm` 보조 버튼으로 밀려 **급한 것이 안 급한 것보다 작게** 보인다.
   - **이미 시작한 학습이면 그렇게 말한다**(A-112). primary는 `이어서 풀기`이고(아직 손대지 않았으면 `시작하기`) 메타 줄은 문항 수 대신 진행을 말한다(`3 / 10 풀었어요` — 풀이 화면과 같은 형식). 분모가 문항 수라 `10문항`을 함께 두면 같은 숫자를 한 줄에서 두 번 말한다. 이어서 푸는 일의 이름은 앱 전체에서 `이어서 풀기` 한 벌이다.
   - **라벨은 상태와 무관하게 `오늘의 학습`이다**(D-169). 담아 둔 학습이 히어로를 차지해도 바꾸지 않는다 — 왜 이 항목인지는 오른쪽 `SourceTag`가 말하고, 라벨을 갈면 그 상태에서만 홈에서 `오늘`을 말하는 글이 사라진다.
   - 약속한 일이 없으면 **오늘 낸 것이 있을 때만** `오늘 할 일을 다 끝냈어요`이고, 없으면 `지금 해야 할 학습이 없어요`다(D-169). 시점 없이 앞 문장만 쓰면 개인 이용권 계정에 **영구 상태**가 되어 홈이 매일 "오늘은 안 해도 된다"고 말한다. 그 아래 한 줄은 **실제로 열려 있는 길만** 가리킨다(D-141): 담아 둔 오답이 있을 때만 오답 복습을, 개인 이용권이 있을 때만 새로 고르기를 말하고, 둘 다 없으면 이유나 지금 기다리는 것을 말한다(`학원에서 과제를 내주면 여기에서 알려 줘요.`).
   - 학습도 담아 둔 것도 없는 계정에는 `아직 시작한 학습이 없어요`를 두고, `문제 담으러 가기`는 **개인 이용권이 있을 때만** 함께 둔다 — 없으면 이유 한 줄로 끝낸다(`개인 학습 이용권이 없어서 아직 고를 수 있는 학습이 없어요.`). 이용권을 시작하는 진입점은 결제 연결과 함께 온다(마스터 플랜 A-096).
2. **진행 상황** — `남은 학습 N개` + 출처별 내역(`학원 과제 4개 · 담아 둔 학습 1개`) + 오른쪽 `완료/전체` 숫자 + 얇은 막대. **분모는 학생이 약속한 일** = 학원 과제 + 담아 둔 학습이다(공개 카탈로그 전체가 아니다 — 공개 개수는 학습 탭이 말한다). 학원 과제는 배정 수가 곧 목표라 완료 후에도 분모에 남고, 담아 둔 학습은 풀면 큐에서 빠지므로 남은 개수로만 센다. **출처를 섞어 세지 않는다** — 합쳐 세면 "학원 과제가 16개"처럼 읽힌다. 개수가 0인 항목은 내역에서 빼고, 둘 다 비면 내역 줄 자체를 두지 않는다(`학원 과제 0개`처럼 읽히지 않게). 셀 것이 없으면 진행 상황과 막대를 감춘다. 같은 숫자를 히어로에서 또 말하지 않는다.
   - **내역 줄은 한 종류만 남아도 그린다**(D-167). 그 줄의 일은 숫자가 아니라 **출처**다 — `남은 학습 1개`만으로는 학원이 낸 것인지 내가 담은 것인지 알 수 없다. 한때 `쪼갤 것이 둘 이상일 때만`으로 좁혔다가 되돌렸다(확정 정책 4절이 따로 세라고 정하고 E2E 둘이 그 계약을 지키고 있었다).
   - **다 끝냈으면 라벨이 `마친 학습`이다**(D-167). 히어로가 이미 `오늘 할 일을 다 끝냈어요`라고 말하는데 여기서 `남은 학습이 없어요`라고 또 하면 같은 사실이 두 번이다 — 그때 이 줄이 말할 것은 얼마나 했는지다.
   - **학원 소속이 없으면 오른쪽 `완료/전체`와 막대를 그리지 않는다**(D-169). 제출하면 서버가 담아 둔 목록에서 세트를 지우므로 그 계정의 분자는 **정의상 항상 0**이다 — `0 / 1 완료`와 전부 회색인 칸은 정보가 아니라 오해다. 왼쪽 두 줄이 말할 것을 다 말한다.
   - **한 칸짜리 막대는 그리지 않는다**(D-167). 셀 것이 하나면 칸이 개수를 말해 주지 못하고 옆의 `0 / 1 완료`가 이미 같은 말을 한다 — 남는 것은 폭만 채운 선 하나다.
3. **학원 과제** — **히어로가 보여 주지 않은 항목이 있을 때만** 둔다(D-167). 제목은 상태 문장이 아니라 목록 이름 `학원에서 내준 과제`다 + 목록(마감이 이른 순, 5줄 + 더 보기, 히어로에 올린 항목은 제외). 히어로가 개인 학습을 가리키고 있으면 섹션 제목 옆에 마감이 가장 이른 과제로 가는 `과제 먼저 하기`를 둔다(히어로가 이미 학원 과제면 같은 행동을 두 번 두지 않는다). 모두 끝냈으면 `학원에서 내준 과제를 모두 마쳤어요.` **한 줄이다**(D-167 · 용어는 `과제`로 통일 — D-169) — 카드가 아니고, `개인 학습 고르기` 버튼도 두지 않는다. 그 버튼의 목적지(`/student/learn`)는 4번의 `문제 담으러 가기`와 **같은 곳**이라 이름만 다른 같은 행동이 한 화면에 둘이었다. 새로 고르는 행동은 4번이 맡는다. **학원 학습이 없는 학생에게는 둘 다 띄우지 않는다**(없는 소속을 있는 것처럼 말하지 않는다).
4. **담아 둔 학습** — 학생이 학습 탭·추천에서 담아 둔 개인 학습 5줄 + `전체 N개`(→ `/student/queue`). 비어 있으면 안내 한 줄 + `문제 담으러 가기`(D-123의 한 벌). 개인 이용권도 담은 것도 없으면 섹션을 감춘다. **히어로가 이미 `문제 담으러 가기`를 내놓는 상태에서도 감춘다**(D-167) — 그러면 처음 온 학생이 없다는 말을 두 번 읽고 같은 버튼을 두 개 받는다. 그 상태에서 담아 둔 목록은 반드시 0개이므로 감춰도 잃는 정보가 없다. 빈 상태는 **`EmptyState`**다(D-104 — 앱에 형태는 하나뿐이다). 앞이 `학원에서 내준 과제를 모두 마쳤어요.` 한 줄일 때만 §6의 예외대로 hairline을 조건부로 둔다 — 그 줄은 스스로 경계선이 없다. 히어로(오늘의 학습)는 이 목록과 남은 학원 과제를 합쳐 **마감이 급한 것부터** 쓴다(1번) — 둘 다 없으면 히어로에 학습을 올리지 않는다(D-140 · D-170).
6. ~~**오답 복습 / 오답노트**~~ — **홈에서 뺐다**(D-132). 오답을 다시 푸는 일은 학습 탭이 맡는다(D-130). 홈은 오늘 할 일을 말하는 곳이라, 이미 푼 것을 다시 하라는 말이 그 아래에 붙으면 오늘 할 일이 둘로 갈린다. 홈이 오답을 가리키는 유일한 자리는 `오늘 할 일을 다 끝냈어요` 히어로의 캡션 한 줄이고, 그 문장도 학습 탭을 가리킨다.

5. **Scody AI** — 버튼이 아니라 입력창을 바로 둔다. 물어볼 말이 있는 학생은 화면을 옮기지 않고 여기서 쓴다(보내면 대화 화면에서 답이 이어진다). **할 일 목록 아래다**(D-167) — 예전에는 2번과 3번 **사이**에 있어서 오늘 할 일을 훑는 시선이 화면 중앙의 큰 입력 박스에 걸려 끊겼다. 물어보는 일은 막혔을 때 하는 일이라 할 일보다 앞에 설 이유가 없다.

주인공만 크게, 나머지는 보조 무게. **카드 3개 나열 금지** — 전부 완료한 학생의 홈에 카드가 넷이었다(실측). 지금은 히어로와 `EmptyState` 둘뿐이고, `학원 과제물을 모두 마쳤어요`처럼 **한 문장인 사실은 카드가 아니라 한 줄**이다.

## 14-1. 학습 탭 · 고르기 · 담아 둔 학습

기준은 `app/student/learn.tsx` · `app/student/pick.tsx` · `app/student/queue.tsx`. 섹션은 **학원 학습 → 개인 학습 → 오답노트** 셋이고(D-130), **담아 둔 학습은 개인 학습 안에 있다**(D-047).

- **담아 둔 학습을 밖으로 빼지 않는다.** 내가 담은 것도 개인 학습이라, 섹션으로 올리면 학원 학습과 같은 층위로 읽힌다. 개인 학습 안에서 `담아 둔 학습 N개가 있어요` 한 줄과 **갈 곳 목록**(`담아 둔 학습 풀러 가기` · `학습할 문제 담으러 가기`)으로 둔다.
  - **갈 곳이 둘이면 버튼을 나란히 두지 않는다.** `Group` + `Row`로 한 줄에 하나씩 고르게 한다(`ActionBar` 규칙 1). 예전에는 두 버튼이 한 줄에 나란히 서 있었는데, 폭이 다른 두 개 중 어느 것이 주 행동인지 모양으로 읽히지 않았다. **순서가 곧 위계다** — 담아 둔 것이 있으면 그것부터 푸는 줄이 위다. 이미 고르는 일을 끝낸 학생에게 다시 고르러 가라고 하는 것이 주된 행동일 수 없다.
  - **다른 화면으로 보내는 버튼의 이름은 한 벌이다**: 담으러 가는 곳은 어디서나 `문제 담으러 가기`(홈 빈 상태·기록 빈 상태·담아 둔 학습 화면), 학습 탭 안에서만 `학습할 문제 담으러 가기`로 목적어를 붙인다. `풀러 가기`는 목록 줄에서 뜻이 서지 않아 스크린리더가 읽던 이름(`담아 둔 학습 풀러 가기`)을 그대로 제목으로 올렸다.
- **학습 탭(`learn`)**: 무엇을 할 수 있는지만 보여 준다. **이 화면에는 R1(화면 primary)이 없다** — 갈 곳이 둘 이상이라 `learn-pick`도 `Group`+`Row`다(D-047). 없는 것이 정상이고, 위계는 섹션 순서가 만든다. **학원 섹션은 소속이 있는 학생에게만 둔다**(`account.academyName`). 소속이 없는 학생에게 빈 학원 안내를 띄우면 화면 맨 위 가장 큰 제목이 자기와 무관한 말이 되고, 이 학생의 첫 행동 줄(`learn-pick`)이 아래로 밀린다(§14의 4번과 같은 규칙 — 없는 소속을 있는 것처럼 말하지 않는다). 소속은 있고 배정만 없는 학생에게는 빈 안내가 사실이므로 그대로 남긴다.
- **고르기(`pick`)**: 학년 → 영역 → 유형 → 목록. 단계는 URL 쿼리에 남기고 뒤로가기가 한 단계씩 물러난다.
  - **고를 것이 0개인 칸은 세 단계 모두 같은 규칙으로 막는다**: chevron을 떼고 `onPress`를 주지 않으며, subtitle을 `아직 준비 중이에요`로 둔다. 색·기호만으로 뜻을 전하지 않고 문장으로 이유를 남긴다. 눌리는데 들어가면 아무것도 못 하는 화면을 만들지 않는다.
  - **지나온 경로는 본문 첫 줄의 `caption`**이다(`Screen`의 `eyebrow`로 넘기지 않는다 — §4의 한글 금지 변형이고 `inkTertiary` 대비가 낮다). 마지막 단계에서 유형 이름을 섹션 제목으로 또 쓰지 않는다 — 경로에 이미 있다.
  - **개인 이용권이 없으면 단계를 그리지 않는다.** 모든 칸이 0개가 되어 이용권 문제를 콘텐츠 문제로 잘못 말하기 때문이다. 학습 탭과 같은 두 문장(학원 결제 / 개인 결제)으로 이용권 상태를 밝히고 학습 탭으로 돌아가는 행동만 둔다.
- **오답노트 섹션**: 학습 탭 맨 아래다(D-130). 이미 푼 것을 다시 하는 일이라 새로 고르는 것보다 뒤다. **기록 탭에 두지 않는다** — 기록은 무엇을 했는지를 보는 곳이다.
  - **담아 둔 오답이 없으면 섹션을 그리지 않는다.** 빈 안내를 두면 소속 없는 학생의 첫 행동 줄(`learn-pick`)이 그만큼 아래로 밀린다(위 `학습 탭` 항목과 같은 규칙).
  - **한 섹션 안에 목적지가 둘이면 목록과 제목 옆으로 가른다.** 목록은 다시 풀 범위 고르기(`/student/review`, 쿼리만 다르다), 질문·메모(`/student/notebook`)는 다른 일이라 `Section`의 `action`이다(§8 ④).
  - **숫자는 `subtitle`이 아니라 `meta`에 한 형식으로**(`오답 N개`). 형식이 갈리면 오른쪽 열을 세로로 훑을 수 없다. 캡션은 목록과 같은 숫자를 다시 말하지 않고 이 목록이 무엇을 고르는 것인지만 말한다(`다시 풀 범위를 골라요.`).
  - **영역이 하나뿐이면 그 줄을 그리지 않는다.** `전체 복습하기`와 완전히 같은 덱이다.
- **담아 둔 학습(`queue`)**: 담은 순서가 곧 풀 순서다.
  - **목록 위에 `첫 번째 학습부터 시작하기` primary 하나.** 이 화면에서 가장 무거운 버튼이 빼기가 되면 순서를 맞춘 노력이 보상받지 않는다. 빼기 모드와 빈 상태에서는 두지 않는다.
  - **여러 개 빼기 버튼은 고른 것이 있을 때만 렌더한다**(D-036과 같은 규칙). 지시문(`뺄 학습을 골라주세요`)을 버튼 라벨에 넣지 않고 상단 캡션(`뺄 학습을 골라요.`)으로 옮긴다.
  - **줄마다 붙는 `위로`·`아래로`·`빼기`는 `ghost` + `size="sm"`에 아이콘을 앞에 둔다**(`arrow-up`·`arrow-down`·`minus-circle`). 글자만 두면 목록 아래 캡션과 구분되지 않아 누를 수 있는 곳으로 읽히지 않는다(§8).
  - **뺀 것은 되돌릴 수 있다**(D-033). 확인 단계 없이 바로 빼고 화면 위에 `담아 둔 학습에서 뺐어요 · 되돌리기`를 남긴다. 원래 자리(여러 개면 자리들)를 함께 기억해 순서까지 복원하고, 마지막 하나를 빼서 빈 상태로 넘어가도 안내는 남긴다. 토스트로 옮기지 않는다(D-038).

## 15. 인증 화면(로그인·가입) 구성

- **로그인 수단의 아이콘은 그 수단의 것을 쓴다**(D-165). 카카오는 **카카오 공식 심볼**
  (`src/components/KakaoSymbol.tsx` — 공식 로그인 버튼 이미지에서 심볼만 잘라 배경을 투명으로
  만든 것, 모양은 손대지 않는다). 범용 말풍선 아이콘을 쓰지 않는다 — 카카오 버튼인데 카카오
  것이 아닌 아이콘이면 무엇으로 들어가는지 모양이 말해 주지 않는다. **쓰는 자리는 카카오 로그인
  버튼 하나뿐이다**(상표라 다른 기능에 돌려쓰지 않는다).
- **아이콘 무게를 맞춘다.** 카카오 심볼이 채워진 도형이라 옆의 휴대폰도 채운 실루엣이다
  (`PhoneMark`). 얇은 선 아이콘 하나만 남으면 같은 크기인데 한쪽만 비어 보인다.
- **내부 로그인은 `/staff`다**(D-165). 화면 어디에서도 링크하지 않고, 아이디와 비밀번호를 받는다.
  주소는 벽이 아니다 — 벽은 Supabase 인증이고 비밀번호는 공개 저장소에 없는 난수다(D-157).
  틀렸을 때 **어느 쪽이 틀렸는지 말하지 않는다**(아이디 존재 여부를 알려 주면 계정을 셀 수 있다).


기준은 `src/features/auth/AuthShell.tsx`. 로그인·가입은 같은 껍데기를 쓴다.

- **한 컬럼(폭 420)을 화면 가운데에.** 넓은 화면에서는 위아래도 가운데. 가운데 정렬은 `justifyContent`가 아니라 자동 여백(`marginVertical: 'auto'`)으로 한다 — 내용이 화면보다 길어지면 `justifyContent: center`는 위쪽을 잘라 스크롤로도 닿을 수 없게 만든다.
- **패널**: 태블릿·데스크톱은 `surface` + hairline + `radius.card` + 패딩 `xxl`. 모바일은 테두리를 없앤다(화면 테두리와 겹쳐 보인다). 그림자는 없다.
- **컬럼 맨 위에 워드마크.** 누르면 나가는 길이다(들어온 화면 → 없으면 소개 페이지). 파비콘 도형은 붙이지 않는다(§4).
- **본문은 좌측 정렬.** 컬럼만 가운데에 두고 안쪽 요소를 중앙 정렬하지 않는다.
- **주요 행동은 `size="lg"` 버튼 하나.** 보조 행동은 전폭 버튼을 더 쌓지 말고 `TextLink`로 내린다.
- **방법이 여럿이면 `LabeledDivider`(`또는`)로 가른다.** 로그인은 `아이디+비밀번호 → 또는 → 카카오`이고
  휴대폰은 로그인 수단이 아니다(D-166). 가입은 그대로 `휴대폰 → 또는 → 카카오`다(D-020·D-125).
  **아직 연결되지 않은 수단에는 버튼을 두지 않는다**(D-141) — 그 자리에 사실 한 줄만 남긴다.
  그러면 방법이 하나뿐인 빌드에서는 `또는` 구분선도 사라진다.
- **아래 블록(`below`)을 펼치면 그 자리로 스크롤한다**(`AuthShell`의 `useAuthReveal`). 인증 패널
  **밖 아래**는 화면 끝에 가까워서, 펼침이 뷰포트 밖에서 일어나면 라벨만 바뀌고 "눌러도 아무 일이
  없다"로 읽힌다(실측). 접힌 라벨에는 개수를 담고(`테스트 계정 11개 보기`) 보이지 않는 알림
  (`LiveRegion`)으로도 같은 사실을 말한다.
- **단계형 흐름에는 진행 표시(`AuthSteps`).** 총 단계 수를 모르는 시점(가입 방법 선택 전)에는 표시하지 않는다.
- **묻는 것이 여러 묶음이면 `AuthSection`**(굵은 소제목 + 회색 한 줄 힌트 + 입력). 한 묶음이면 `Field`의 라벨만으로 충분하다 — 같은 말을 두 번 쓰지 않는다.
- **푸터에 문서 링크와 테마.** 로그인 전에도 약관·개인정보처리방침을 열 수 있어야 한다.

참고: 오늘의집 로그인·가입 화면의 구성 원칙(좁은 중앙 컬럼, 한 면에 모은 입력, 라벨 붙은 구분선, 글자 링크 보조 행동, 항목 제목 + 힌트). 자산·문구는 가져오지 않는다.

## 16. 문제 풀이 화면

기준은 `app/student/solve/[id].tsx`.

- **지문은 하나의 면**(`Passage`: surface + hairline + `radius.card`). 문제 카드는 그 면 밖에 둬서 읽는 곳과 푸는 곳을 가른다. 지문 본문은 단락(`\n`)마다 끊어 놓는다.
- **데스크톱 2단에서 지문은 화면에 붙어 있는다**(웹 `position: sticky`, `maxHeight: 85vh`). 문항을 내려 보다가 지문을 다시 찾아 올라가게 만들지 않는다.
- **한 화면에 최대 5문항.** 10문항을 한 번에 쏟으면 어디를 푸는지 놓친다. `한 문항씩` 모드도 제공한다(`SegmentedControl`) — 한 문제만 두고 `다음`으로 넘어간다.
- **문항 번호와 발문은 두 컬럼**: 번호는 26px 원형(`offset`)으로 왼쪽에 고정하고 발문은 오른쪽에서 접힌다. 번호와 발문을 한 문장(`1. 발문`)으로 두면 발문이 길어질 때 번호 아래로 흘러 줄이 어긋난다.
- **선지는 여러 줄을 기준으로 설계한다**: 행 정렬은 `flex-start`, 동그라미는 첫 줄에 고정, 본문 줄간격 1.55. 실제 국어 선지는 두 줄 이상이 흔하다.
- **선택된 선지**는 `accentSoft` 배경 + `accent` 테두리 + medium 무게. 색만으로 알리지 않는다(동그라미가 함께 찬다).
- 상단에는 `풀었어요` 개수와 진행 막대만 둔다. **제출 버튼은 다 풀었을 때만 나타난다**(라벨은 늘 `제출할게요` + `trailing` 화살표). 남은 개수는 상단 진행 표시가 이미 말하므로 버튼을 흐리게 띄워 두고 눌렀을 때 이유를 알리지 않는다 — 마지막 답을 고르는 순간 버튼이 나타나는 것이 '다 했다'는 가장 분명한 신호다(D-036).
- 보기 방식 전환도 `SegmentedControl`이다(§8). 문항 이동은 `이전`·`다음` 버튼과 `현재/전체` 표시로 한다. **두 버튼은 같은 무게로 둔다** — 폭뿐 아니라 `variant`도 같아야 한다(둘 다 `secondary`). 문항 화면의 primary는 `제출할게요` 하나이고 **그 버튼이 나타나는 것 자체가 다 풀었다는 신호**라, `다음`이 accent면 어느 쪽이 끝인지 알 수 없다. 좌우 칸이 같은 `flex: 1`이면 각 칸에 `alignItems`(`flex-start`·`flex-end`)를 함께 줘야 한다. 빠뜨리면 기본값 `stretch`로 그 버튼만 칸 전체로 늘어난다.
- `다음`은 마지막 페이지에서 **DOM에서 사라진다**(`disabled`로 두지 않는다). `e2e/_solve.ts`의 `answerAll`이 이걸 종료 신호로 쓴다.
- **다시 풀기로 들어오면 지난 답이 지워진 채로 시작한다**(`?retry=1`, D-124). 그러지 않으면 이전 선택이 그대로 칠해져 있고 제출 버튼이 처음부터 떠 있어, 문제를 하나도 다시 읽지 않고 같은 점수를 새 기록으로 덮을 수 있다 — 바로 위 D-036의 신호가 재풀이에서만 뜻을 잃는다. `이어서 풀기`(D-035)는 반대로 답이 남아 있어야 하므로 표시를 달지 않는다.

## 17. 결과·오답노트 화면

- **이 흐름의 이름은 둘뿐이다**(D-150). 질문하고 메모하는 곳은 **`오답노트`**(`/student/notebook`), 다시 푸는 곳은 **`카드 복습`**(`/student/review`)이다.
  - **오답노트로 가는 행동은 어디서나 `질문하고 메모하기`다** — 학습 탭 오답노트 섹션 제목 옆, 결과 화면 담기 섹션 제목 옆, 두 자리 모두. 두 섹션 제목이 이미 `오답노트`를 말하므로 버튼이 목적지를 다시 부르지 않는다(예전 `오답노트 하러 가기`).
  - **카드 복습의 화면 이름은 하나이고 범위를 앞에 붙인다**: `카드 복습` / `별표 카드 복습` / `{영역} 카드 복습`. 덱을 고르는 줄 이름은 D-130 그대로다(`전체 복습하기` / `별표 친 것만 복습하기` / `{영역} 복습하기`).
  - **메모의 이름은 `내 오답노트 메모` 하나다.** 카드 복습에서 물어보는 섹션도 `질문하고 메모하기`라고 부른다(예전 `더 파고들기`) — 그 안에서 하는 일이 오답노트와 같다.

- **카드 복습의 카드 구성은 한 순서다**: `정답 → 해설 → (학원 고지) → 메모 → 이해 완료`. 답을 고르면 정오 한 줄(`이번엔 맞혔어요`/`아직 헷갈려요`) 다음에 `정답 · {선지}` → `처음 풀 때 고른 답 · {선지}` → **`해설`** → 학원 출처면 공개 고지(D-054) → `내 오답노트 메모`(없으면 물어보라는 한 줄) → `이제 이해했어요`가 온다. 질문(`질문하고 메모하기`)은 그 아래, **해설을 읽은 다음 단계**다.
  - **해설은 접지 않는다.** `Passage`를 접는 이유는 지문이 여러 단락이라 발문·선지를 화면 밖으로 밀기 때문인데, 해설은 한두 문장(seed 192문항 전부가 있고 최대 40자)이고 `왜 틀렸는지 알고 싶다`가 이 화면에 온 목적이다 — 목적을 한 번 더 눌러 펼치게 만들지 않는다.
  - **해설이 등록되지 않은 문항에서는 그 자리를 비운다.** 답을 고르기 전 안내도 그때는 `정답과 내 메모를`이라고만 말한다 — 없는 것을 있다고 약속하지 않는다(§9의 D-133과 같은 규칙).
  - 이름과 글자 무게는 결과 화면(`QuestionReview`)과 같다 — 같은 문항의 같은 글이다. 읽어야 하는 문장은 `tertiary`로 두지 않는다(대비 3.23:1, AA 미달 — §8의 `meta` 규칙과 같은 이유).
  - **빈 덱의 `학습으로 돌아가기`와 복습을 끝낸 뒤의 `학습으로 돌아가기`는 다른 행동이다**: 앞은 `EmptyState`의 `hug`(아직 할 것이 없다), 뒤는 화면을 끝내는 전폭 primary. 모양이 다르므로 `testID`도 갈라 둔다(`review-to-learn` / `review-done-to-learn`).

- **결과의 문항 리뷰는 기본이 '틀린 문항'**이다. 전체는 `SegmentedControl`로 펼친다. 다 맞았으면 고를 것이 없으니 전체를 보여준다.
- **문항 리뷰도 다섯 개까지다**(§8의 5줄 상한, D-144). 그 이상은 섹션 제목 옆 `N개 더 보기`(R2 한 벌)로 펼치고 `접기`로 되돌린다. `QuestionReview`는 발문 + 내 답 + 정답 + 해설이 든 큰 덩어리라 상한이 없으면 이 섹션이 아래 두 섹션(담기 목록·추천)을 화면 밖으로 밀어낸다 — 390 실측: `전체 10문항`에서 `오답노트 하러 가기`가 2456px(2.91화면) → 1613px(1.91화면). 필터를 바꿔도 펼친 상태는 되감지 않는다.
  - **담기 목록에는 상한을 두지 않는다.** 그 목록이 그 섹션의 목적이고, 진입 버튼(`오답노트 하러 가기`)보다 아래에 있어 주 행동을 밀어내지 않는다. 여기에 상한을 두면 여섯 번째 오답을 담으려면 먼저 펼쳐야 한다.
- **오답노트 담기는 아이콘 토글**(`bookmark` → `check` + `accentSoft`). 행 전체를 누르게 두면 눌릴 때 배경이 번쩍인다 — 누를 곳은 토글 하나로 좁힌다. 다시 누르면 노트에서 빠진다.
- **오답노트 문항 카드는 두 덩어리다**: `문항 카드[출처 + 헤더 + 내 답·정답 + 고지 + 메모 + 다시/이어서 정리]` → `대화 카드[로그 + 입력창]`. **대화가 위, 입력창이 아래** — 챗 서비스처럼 방금 쓴 말 바로 아래에서 이어 쓴다. 대화가 없어도 대화 카드를 띄워 물어볼 곳을 늘 열어 둔다.
  - 화면 하단 고정(대화 화면 방식)은 쓰지 않는다. 문항마다 카드와 대화가 따로라 하단에 하나를 고정하면 어느 문항에 쓰는 건지 정의되지 않는다.
  - **카드 첫 줄은 `SourceTag`다**(§18). 이 목록에는 개인 학습 오답과 학원 과제 오답이 섞인다. 학원 출처 노트에는 `학원 과제에서 담은 오답의 메모는 선생님이 볼 수 있어요.`를 정답 줄 아래 캡션으로 둔다(D-110·D-054) — 메모가 아직 없을 때에도 둔다. 개인 학습 노트에는 붙이지 않는다.
  - 헤더 아이콘 순서는 `[발문] [노트] [별표] [휴지통]`. 노트를 별표 왼쪽에 둬야 "별표 옆 휴지통"(D-033)이 그대로 성립한다.
    - **좁은 컬럼에서는 아이콘 줄을 발문 아래로 내린다**(D-109, `useColumn().isMobile`). 발문 옆에 44px 셋을 두면 390에서 발문에 약 180px만 남는다. 한 줄로 둘 때는 `alignItems: flex-start`로 아이콘을 **첫 줄에 고정**한다 — 세로 가운데에 두면 여러 줄 발문의 가운데에 아이콘이 걸려 스크롤하다 스친다. 휴지통은 파괴적이므로 옆 아이콘과 `spacing.sm` 이상 뗀다.
    - 폭 판단은 **`Screen`의 컬럼 안에서** 해야 한다. 화면 함수 본문에서 부른 `useColumn()`은 `ColumnWidthProvider` 밖이라 창 폭으로 되돌아간다 — 그 줄만 담당하는 작은 컴포넌트를 화면 파일 안에 둔다(`notebook.tsx`의 `NoteHead`).
  - **정리 아이콘은 대화가 있을 때만 보인다.** 늘 띄우면 물어보지 않은 문항에서 눌러도 아무 일이 없는 죽은 버튼이 된다.
  - 정리 결과는 토스트(`노트에 정리했어요`)로 알린다. 아이콘 모양만 바뀌면 저장됐는지 확신이 안 선다. **진행은 라벨이 아니라 캡션이다**(`정리하는 중이에요`, §8) — 안내 문구를 버튼 이름 자리에 넣지 않는다.
  - **답을 기다리는 동안 진행이 보인다**(D-108): 첫 조각이 오기 전에도 `Scody AI · 답을 쓰고 있어요`를 그리고, 작업 중에는 그 화면의 보내기 버튼을 전부 끈다(한 번에 한 문항만 부른다).
  - **AI 실패는 대화에 넣지 않는다**(D-107): 그 카드 안 인라인 캡션(`tone="danger"`, `지금은 답하지 못했어요. 잠시 뒤 다시 물어봐 주세요.`)으로 말하고 **쓴 질문은 입력창에 그대로 남긴다**. 개발자용 오류 문구(`Scody AI 연결 오류: …`)를 학생 화면에 내보내지 않는다.
  - **마무리 카드는 필터와 무관한 전체를 센다**(D-105). 좁혀 본 영역만 끝났으면 두 문장으로 말한다 — `문법 오답은 다 정리했어요.` + `다른 영역에 3개 남아 있어요.`
  - **정리는 늘 대화 전체를 다시 요약해 새로 쓴다. 뒤에 이어 붙이지 않는다** — 요약이 언제나 전체를 보므로 붙이면 같은 내용이 두 번 쌓인다. 메모 아래 두 행동은 `추가로 대화한 내용까지 더해서 정리하기`(전체 재요약, `ghost` + `refresh-cw`)와 `정리와 대화 지우기`(초기화, `secondary` + `tone="danger"` + `ConfirmStep`)로, 무엇이 달라지는지 라벨에서 갈린다. **둘 다 노트마다 반복되므로 기본 `md`(44)다** — `sm`은 섹션 제목 옆 전용(§8·§10). `ghost`의 정렬 상쇄 마진은 행이 아니라 **그 버튼에** 둔다 — 행에 두면 줄바꿈한 둘째 줄의 테두리가 카드 선에 닿는다.
- **목록 행에 발문만 두지 않는다.** 결과 화면의 오답 담기 목록처럼 같은 발문이 반복되는 세트(문법 은행)에서는 모든 줄이 똑같아 보인다. 정답 선지를 `subtitle`로 함께 보여 어느 문제인지 구분되게 한다.
- **지문이 여럿인 화면은 첫 것만 펼친다**(`Passage`의 `collapsible` + `defaultOpen`). 다 펼치면 화면이 지문으로 덮여 정작 볼 오답이 묻힌다. 접을 때는 본문을 **렌더하지 않는다** — 높이만 0으로 감추면 감춘 글자까지 검색에 걸린다.
- **되돌릴 수 있는 삭제는 확인 단계를 두지 않는다.** 오답노트 문항 지우기는 별표 옆 휴지통 → 바로 빠지고, 화면 위에 `오답노트에서 뺐어요. · 되돌리기`가 남는다(원래 자리로 복원). 문장형 확인 버튼(`메모까지 지울게요` / `그대로 둘게요`)은 쓰지 않는다 — 버튼인지 안내인지 구분되지 않는다.
- **담기 토글은 두 가지**를 구분한다: 오답노트 문항 담기는 `bookmark → check`(이름 `오답노트에 담기`), 학습 담기는 `plus → check`(이름 `담아 두기`). 아이콘과 읽히는 이름이 모두 달라야 한다.
- **대화 입력창**(`AskField`)은 카드 안(`flat`)에서 자기 테두리를 긋지 않는다. 구분선은 `Group`이 이미 그으므로 겹치면 선이 두 겹으로 보인다. 포커스는 브라우저 기본 링을 끄고 `ink` 테두리(카드 안이면 `offset` 배경)로 알린다 — 기본 링은 사각형이라 둥근 모서리와 어긋난다.
- **담기·빼기는 `Toast`로 한 줄 알린다**(`문항을 오답노트에 담았어요` / `오답노트에서 뺐어요`). 아이콘 모양만 바뀌면 담겼는지 확신이 서지 않는다. 지우기의 `되돌리기`는 토스트로 옮기지 않는다 — 사라지면 되돌릴 기회도 사라진다.

## 17-1. Scody AI 대화 화면

- **입력창은 화면 아래에 고정한다.** 대화만 스크롤되고, 새 답이 오면 맨 아래로 따라간다. 대화가 아래로 쌓이는데 입력창이 위에 있으면 쓸 때마다 눈이 위아래를 오간다.
- 이 화면만 **`Screen`을 쓰지 않는다.** 입력창이 스크롤 영역의 형제여야 하고(`Screen`은 전체가 하나의 `ScrollView`다), 맨 아래로 따라가려면 `ScrollView` ref가 필요하다. 구조는 `KeyboardAvoidingView(flex:1)` → 고정 헤더 / `ScrollView(flex:1)` / 하단 바 3단이고, 세 층이 같은 폭 컬럼(`contentMaxWidth`)을 써서 좌우 정렬선이 다른 화면과 어긋나지 않게 한다. 웹에서 `position: fixed`는 쓰지 않는다 — 루트의 높이 체인(`html,body,#root`)이 이어져 있어 flex만으로 성립한다.
- **하단 탭은 이 경로에서 숨긴다**(`RoleShell`의 `chatting`). 입력창과 겹치기 때문이다. 문제 풀이 몰입 모드와 달리 **데스크톱 사이드바는 그대로 두고**, 나가는 길은 화면이 그리는 좌상단 뒤로가기다.
- **보낸 질문은 답을 기다리는 동안에도 보인다**(`pending`). 답이 끝난 뒤에야 화면에 올리면 홈에서 넘어왔을 때 내 질문이 사라져 흐름이 끊긴다.
- **사용자가 위로 올려 읽는 중이면 끌어내리지 않는다**(하단 64px 이내일 때만 따라간다). 내가 보낸 말은 예외로 항상 맨 아래로 간다.
- 헤더와 하단 바에는 hairline 구분선을 둔다. 대화가 그 사이를 지나가므로 경계가 없으면 글이 제목에 닿아 보인다.

## 18. Academy assignment vs personal study distinction

- 데이터: `LearningItem.source` = `academy | personal`. UI는 항상 `SourceTag`(작은 점 + 텍스트).
- 학원 과제: 마감·배정 맥락. 학원 계정은 배정 학습과 결과만 열람(개인 학습 상세 접근 금지).
- 개인 학습: 학생/학부모 결제, 마감 없음. 공개 콘텐츠에서 파생.
- 학습 탭에서 두 출처는 **별도 섹션**으로 분리. 색만이 아니라 라벨로 구분. 두 이용권 병존 가능(내 정보에 함께 표기)./

### 18-1. 학습 배정 화면 (단계형)

기준은 `app/academy/assign.tsx`(D-062).

- **한 화면에 다 쏟지 않고 `반 → 학습 → 확인` 세 단계로 나눈다.** 단계는 URL 쿼리(`?class=&grade=&area=&topic=&content=&due=`)에 남기고, 이탈·되돌아가기는 `Screen`의 표준 뒤로가기가 맡는다(`← 다시 고르기` 같은 버튼을 만들지 않는다 — D-039와 같은 규칙). **첫 단계는 탭의 시작점이라 뒤로가기를 두지 않는다.**
- **다른 사람에게 즉시 전달되는 쓰기 행동에는 기본 선택을 두지 않는다.** 목록의 첫 항목을 미리 골라 두면 화면을 구경하다 누른 버튼이 반 전원의 홈을 바꾼다.
- **긴 목록은 `SegmentedControl`(범주) + `Field`(검색) + `Pager`(10개)** 조합으로 좁힌다(선례: `app/admin/users.tsx`). 반이 100개를 넘는 학원에서 이 세 가지가 없으면 목록이 5,000px가 된다.
- **분류 드릴다운은 `학년 → 영역 → 세부 유형`이고 0개인 칸은 막는다**(chevron 없음 · `onPress` 없음 · `아직 준비 중이에요`, D-042). 드릴다운과 **제목 검색을 함께** 둔다 — 검색은 분류를 무시하고 전체에서 찾고, 그 사실을 한 줄로 밝힌다. 분류가 비어 콘텐츠가 드릴다운에 나타날 자리가 없으면 개수를 알려 준다(감추지 않는다).
- **콘텐츠 행은 출처를 항상 말한다**(`우리 학원` / `스코디 제공`, `meta`). 출처 필터는 우리 학원 콘텐츠가 실제로 있을 때만 둔다 — 결과가 0인 필터는 누를 곳이 아니다.
- **확인 단계는 무엇을 · 누구에게 · 언제까지를 요약 한 덩어리로 말한다.** `Group` + `Row` 세 줄이고 학습 제목은 값이 아니라 그 배정의 이름이라 **행 제목**으로 둔다 — 긴 제목을 `trailing`에 넣으면 390에서 옆 설명이 200px로 눌려 `10문항`이 글자 사이에서 끊긴다(§19의 같은 실측 근거). 짧은 값(`반`·`마감`)만 `trailing`의 `label`이다.
- **날짜는 타이핑만 시키지 않는다.** 빠른 선택 `SegmentedControl`(`오늘`·`내일`·다가오는 금요일·`마감 없음`)를 먼저 두고 직접 입력을 아래에 둔다. 검사는 `parseDueDate` 한곳에서 하고(형식·없는 날짜·과거 날짜) 요약 줄이 지금 값을 그대로 비춘다.
- **완료 화면은 요약을 남기고 되돌리기를 화면에 둔다**(`방금 배정한 것 되돌리기`, D-033). 완료 화면의 행동은 전부 `hug`다 — 화면의 목적은 이미 끝났다(D-047).

### 18-0. 학원 대시보드

기준은 `app/academy/index.tsx`(D-078). 집계는 전부 `src/features/academyStats.ts`에서 온다(D-061).

- **숫자와 추이가 먼저다.** 순서는 `고지 → 기간 토글(4·12·26주) → 지표 표 → 추이선 → 확인이 필요해요 → 반별 표 → 영역별 → 학년별/오답 문항 → 분포 → 안 낸 학생 → 지표 정의`. 화면에 **기간 토글은 하나뿐**이다.
- **지표는 표다**(지표·값·변화·추이). 값은 오른쪽 정렬 + 등폭이고, 만들 수 없는 행은 `추이 없음`이라고 **글자로** 적는다(D-076). 표 전체에 추이가 없으면 `변화`·`추이` 열을 만들지 않는다.
- **추이는 끝난 주까지만 그린다**(`lastCompleteWeek`). 진행 중인 이번 주를 넣으면 마지막 점이 늘 바닥으로 떨어져 "제출률이 무너졌다"로 읽힌다(실측 `72,75,73,75,69,68,71,16`).
- **비율 비교는 가로 막대**(라벨 / `ProgressBar` / 값)다. 도넛·게이지·레이더를 쓰지 않는다.
- **빈 칸을 지우지 않는다.** 영역 4개는 콘텐츠가 0세트인 `화법과 작문`까지 자리를 남기고 `콘텐츠 없음`이라 적는다. 분포는 0명 구간을 그대로 둔다 — 분모를 지우면 분포가 목록이 된다.
- **합성 데이터임을 밝힌다.** 추이 블록 아래에 `SourceBadge source="합성"`과 한 줄 설명을 둔다(D-069·D-079).

### 18-2. 반·학생 관리 화면

기준은 `app/academy/classes.tsx` · `app/academy/classes/[id].tsx` · `app/academy/classes/students.tsx` · `app/academy/classes/student/[id].tsx`(D-063·D-080·D-081). 상태와 권한은 `src/features/academy.tsx`에서 온다.

- **여러 대상을 나란히 놓는 목록은 `Table`이다**(D-078). 반 목록은 `반 / 담당 / 학생 / 배정 / 제출률 / 평균 정답률`이고 학생 목록은 `이름 / 반 / 배정 / 제출률 / 평균 정답률 / 안 낸 과제 / 최근 제출`이다. **정렬은 화면이 쥐고 전체를 줄 세운 뒤 페이지를 자른다**(D-082).
- **합계 행은 그 값이 덮는 범위를 이름으로 말한다**(D-084) — 원장 `학원 전체`, 선생님 `담당 반 합계`. 값은 `classesFor(account)`가 준 반에서만 나오므로 이름이 범위와 어긋나면 담당 반 84명이 학원 전체 3,002명으로 읽힌다.
- **눌리는 표는 마지막 열로 그 사실을 말한다**(D-084): `{ key: 'go', header: '', width: 24, cell: () => <Icon name="chevron-right" size={18} color={colors.inkTertiary} /> }`. `Row`의 `showChevron`과 같은 뜻이고 `priority`를 주지 않아 390에서도 접히지 않는다. 대시보드 반별 현황·반 목록·학생 목록 세 표가 같은 형태다.
- **화면의 존재 이유가 걸린 열은 모바일에서 접지 않는다**(D-084). 학생 전체 보기는 "이름만 아는 학생이 어느 반인지" 찾는 화면이라 `반`이 `priority: 1`이고, 접을 순서는 `안 낸 과제`(2)로 내렸다 — 기본 정렬이 안 낸 과제 순이라는 사실은 표 위 캡션이 이미 말한다.
/- **학생 상세는 학원이 배정한 것만 연다**(D-080). 개인 학습·개인 오답노트·별표는 어떤 경로로도 넣지 않고, 화면 첫 줄이 그 사실을 밝힌다.

- **쓰기 행동은 원장 화면에만 둔다.** 선생님 화면에는 담당 반과 학생, 배정으로 가는 길만 있다(3절 권한). 감추는 것은 화면 정리이고 판단은 `useAcademyStaff`의 쓰기 함수가 한다.
- **만들기·이름 바꾸기는 접어 둔다.** 목록이 화면의 목적이므로 입력은 눌렀을 때만 펼치고, 그 안의 제출 버튼과 `취소`는 나란히 `hug`다.
- **긴 후보 목록은 검색 + 6~8개만.** 선생님 60명·학원 학생 3,000명을 고르는 자리에서 전량을 늘어놓지 않는다. 몇 명 중 몇 명을 보여 주는지 캡션으로 밝히고, 학생 추가는 **검색어를 받기 전에는 목록을 그리지 않는다**.
- **고른 항목은 색이 아니라 글자로 말한다** — `trailing`에 `고름`·`지금 담당`(`label` + `tone="accent"`).
- **목록 행의 값은 `trailing`의 `label` 하나다.** 학생 행은 `안 낸 과제 N건`이고 부제는 제출 요약(`제출 1/1 · 평균 80%`)이다. **`빼기` 같은 행동을 값 옆에 함께 두지 않는다** — 값(약 95px)과 버튼(약 76px)이 1280에서도 두 줄로 갈려 행 높이가 제각각이 되고 390에서는 이름 칸이 100px로 눌렸다(실측). 행동은 행을 눌러 펼친 자리에 둔다.
- **펼친 자리에는 그 사람에 대한 사실만 둔다**: 우리 반 배정별 결과 · 배정 학습 오답노트 수 · `개인 학습 기록은 학원에 공개되지 않아요.` · (원장) `이 반에서 빼기`. 학원이 볼 수 없는 것을 이 자리에서 열지 않는다(D-014).
- **되돌릴 수 있는 것과 없는 것을 다르게 다룬다.** 학생 빼기는 확인 없이 실행하고 섹션 맨 위에 `되돌리기` 안내를 남긴다(D-033 · `app/student/queue.tsx`와 같은 모양). 폐강은 되돌릴 수 없어 확인 문장 + `tone="danger"` + `minus-circle`이다.
- **할 수 없는 일은 버튼을 두지 않고 이유를 한 줄로 말한다**(`여기서 만든 반은 아직 배정 화면에서 고를 수 없어요.`). 죽은 버튼을 두지 않는 규칙은 D-036·D-042와 같다.

### 18-3. 학원 문제 화면 · 문제 등록 폼

기준은 `app/academy/content/index.tsx` · `app/academy/content/[id].tsx` · `src/components/ContentComposer.tsx`(D-064).

- **메뉴는 행동이 아니라 대상이다.** `문제 등록`이 아니라 `문제`(우리 학원 콘텐츠)를 두고 등록은 그 화면의 버튼으로 내린다(운영자 메뉴가 이미 같다 — D-017). 라벨이 짧아지면 390 하단 탭(칸당 약 62px)도 함께 나아진다.
- **목록은 우리가 만든 것만.** 운영자 공개 콘텐츠를 섞지 않는다 — 두 출처를 함께 고르는 화면은 학습 배정이고(§18-1) 이 화면은 "우리 학원이 등록한 것"을 센다. 그 사실을 캡션 한 줄로 밝혀 목록이 비어 보이는 이유를 남긴다.
- **좁히는 도구는 목록이 한 화면을 넘을 때만 둔다**(`> Pager` 한 페이지). 결과가 0인 필터는 누를 곳이 아니다(§18-1의 출처 필터와 같은 규칙).
- **행의 값은 `trailing`의 `label` 하나**(`배정 N회`)이고 부제는 분류다(`고2 · 문법 · 어문 규정 - 맞춤법 · 문법형 · 1문항`). `trailing`이 있으면 chevron을 두지 않는다(§8).
- **고칠 수 없는 것은 버튼을 두지 않고 이유를 말한다.** 이미 배정한 학습을 고치면 학생이 푼 기록과 어긋나고, 수정·삭제 API 자체가 없다. 상세 첫 줄이 둘 중 어느 상황인지 문장으로 말한다.
- **등록 폼은 검사를 첫 실패에서 멈추지 않는다.** 문법 은행 세트는 20~25문항(문항당 약 400px)이라 화면 맨 아래 캡션 한 줄로는 어디가 비었는지 찾을 수 없다. 남은 입력을 **해당 필드 아래 인라인**으로 모두 표시하고, 문항 제목에 `N번 문제 · 입력이 남았어요`를, 화면 위에 `아직 입력이 남았어요. 1번 · 3번 문제를 확인해 주세요.`를 둔다. 진행은 `N문항 · 입력 완료 M`으로 위에서 센다.
  - **등록을 누르기 전에는 빈 칸을 오류로 말하지 않는다**(`checked` 플래그). 누른 뒤에는 채우는 대로 오류가 사라진다.
- **하나를 고르는 컨트롤은 손으로 그리지 않고 `SegmentedControl`을 쓴다.** 유형·학년·영역·세부 유형은 물론 **문항의 정답**도 `SegmentedControl`(`보기 1`…`보기 4`)이다 — 입력창을 감싼 `Pressable`은 글자를 쓰려고 눌렀을 때 정답이 바뀐다. 고른 보기의 입력 라벨에 `· 정답`이 함께 붙어 색만으로 뜻을 전하지 않는다.
- **문항 지우기는 확인 없이 실행하고 원래 자리로 되돌린다**(D-033). 안내는 **지운 자리에 그대로** 남긴다 — 25문항 폼에서 화면 맨 위에 두면 무엇을 지웠는지와 멀어진다. 오답노트 지우기와 달리 알림으로 옮기지 않는 이유가 이것이다(D-091은 오답노트에 대한 결정이다).
- **전폭은 `등록할게요` 하나다.** `문제 추가하기`·`이 문제 지우기`·완료 화면의 버튼은 `hug`이고 `ghost`에는 `leading` 아이콘을 둔다(D-047, §8).
- **임시저장을 만들지 않았으면 그 한계를 화면 첫 줄에 밝힌다**(`등록하기 전에 이 화면을 벗어나면 쓴 내용은 남지 않아요.`). 저장되는 것처럼 보이게 두지 않는다.
- **완료 화면은 다음 화면으로 이어 붙인다.** 방금 만든 세트를 `onDone(created)`로 넘겨 학원은 배정(`?content=`)으로, 운영자는 대시보드로 간다. 완료 화면 버튼 라벨은 목적지를 아는 호출부가 정한다(`doneLabel`).

---

## 19. 학부모 리포트

기준은 `src/components/ChildReport.tsx` · `app/parent/report.tsx` · `app/parent/attempt.tsx`. 계산은 전부 `src/features/report.ts` 한곳에서 온다.

- **지표를 카드로 만들지 않는다**(D-050). 지금은 없어진 `StatTiles`가 `flexBasis:220`이라 390에서 한 줄에 한 칸씩 쌓여 지표 7개가 734px — **한 화면(약 732px) 전체**를 먹었다(실측). 리포트에서는 `Group` + `Row` 한 줄로 둔다: **이름(왼쪽) · 지난달 비교(이름 아래 caption) · 값(오른쪽 `trailing`, `label`)**. `meta`는 화면에서 가장 흐린 글자라 값에 쓰지 않는다. **같은 판단을 운영자 화면에도 적용했다**(D-068, §20) — `StatTiles`는 이제 계정 목록(`/admin/users`)과 학원 대시보드에만 남는다.
  - 바로 아래 목록의 개수를 세는 지표는 **한 줄 문장**으로 둔다(`담은 오답 3개 · AI와 정리 2개 · 별표 1개`). 지난달 비교가 붙는 지표만 목록 행으로 만든다.
  - **목록 행에 `leading` + `meta` + 버튼을 함께 두지 않는다.** 390에서 본문이 109px로 눌려 제목이 두 줄로 접힌다(실측). 정답률 같은 값은 `meta`가 아니라 subtitle 맨 앞에 둔다 → 185px.
- **개인 학습과 학원 과제는 섹션을 갈라 놓는다**(D-050). 합쳐 하나의 정답률로 말하면 그 값이 학원 성적인지 집에서 푼 것인지 알 수 없어 학원 상담에 쓸 수 없다. 합쳐도 사실인 것(공부한 날·학습 시간)만 위에 두고 **정답률은 각 출처 블록에만** 둔다.
- **반 비교(등수)는 학원 과제에만 둔다.** 학원 과제에는 반 학생 전원의 제출 정답률이 있어(`Submission.accuracy`) 반 평균과 순위가 **실제 계산**된다. 개인 학습에는 또래 집단이 없으므로 만들지 않고 자기 지난달과만 견준다.
  - **제출자가 `RANK_MIN_SUBMITTERS`(5)명 미만이면 아무것도 보여 주지 않는다.** 2명이면 평균과 내 점수로 다른 학생 점수가 역산되고, 1명이면 자기 점수가 반 평균으로 표시된다.
  - **월 단위 집계 순위를 만들지 않는다** — 과제별 순위를 평균 내면 없는 수가 된다. `반 평균보다 높았던 과제 2/4`처럼 센 값으로 말한다.
  - 평가 문구(`잘하고 있어요`·`상위 30%`)를 붙이지 않는다. 순위는 과제 행의 보조 줄이고 홈에는 올리지 않는다. 반 학생의 이름·개별 점수는 어떤 경로로도 학부모에게 내려가지 않는다(내려가는 것은 `평균 · 제출자 수 · 내 순위` 셋뿐).
- **담은 오답은 하나씩 넘겨 다 볼 수 있다.** `Pager`를 `pageSize={1}`로 쓴다(`3개 중 1–1` + 이전/다음). 한 개만 보여 주고 끊지 않는다.
- **`이해 완료`를 지표로 쓰지 않는다.** 학생이 카드 복습에서 스스로 누른 자기 신고이고 실제로 다시 풀어 맞혔는지와 무관하다. 검증되지 않은 값을 학부모 지표 자리에 두지 않는다(`WrongNote.mastered` 필드는 학생 화면이 계속 쓴다).
- **AI가 실패하면 그 문장을 저장하지 않는다**(D-052). `askScodyAI`는 실패도 문장으로 돌려주므로, 저장하거나 판단이 필요한 곳은 `askScodyAIResult`로 성공·실패를 값으로 받는다. 실패하면 계산된 숫자로 만든 대체 문장을 쓰고 그 사실을 밝힌다 — **개발자용 오류 문구(HTTP 코드·모델명·응답 본문)를 사용자 화면에 내보내지 않는다.** 저장할 때 `byAI`를 함께 남겨 캡션이 렌더 시점 키 상태에 흔들리지 않게 한다.
- **지표 이름과 값의 계산 방식을 맞춘다**(D-052). 자녀 정답률이 문항 가중이면 반 평균도 문항 가중으로 낸다 — 한쪽만 단순 평균이면 `75% · 반 평균 72%`의 부호가 실제와 뒤집힌다. 정답 수는 정답률에서 되돌리지 않고 저장된 값을 쓴다(`accuracy`는 이미 반올림된 값이라 세트마다 ±1이 쌓인다).
- **기간 지표는 기준을 하나로 둔다**(D-052). 그 달 배정은 마감월로만 판정한다 — 제출월과 OR로 묶으면 한 과제가 두 달에 계상되고 그 달에 내지 않은 과제를 `모두 냈어요`라고 말한다. 판정할 수 없는 것(마감일 없는 배정)은 분모에 넣지 않고 개수를 밝힌다.
- **주간 요약에는 주간 값만 넘긴다.** 월 집계를 넘기면 모델이 월 성취를 주간 사실로 서술한다.
- **달과 무관한 행동을 달 블록 안에 두지 않는다.** 칭찬은 달과 관계가 없으므로 `이번 주 요약` 밖에 둔다 — 안에 두면 지난달 리포트에서 통째로 사라진다. 같은 이유로 `자세히 보기`에는 달 무관 미제출 목록을 두지 않는다(6월 상세에서 7월 미제출이 6월 것으로 읽힌다).
- **심화 진입점은 첫 화면에 둔다.** `자세히 보기`를 문서 맨 아래에 두면 390에서 y≈2,400으로 내려가 발견되지 않는다. 첫 월 지표 섹션의 `action` 슬롯이 제자리다(실측: y≈374).
- **되돌릴 수 없는 쓰기를 펼침의 유일한 출구로 두지 않는다.** 칭찬 종류를 펼쳤을 때 `닫기`를 함께 둔다.
- **맨 위는 `이번 주 요약`이다**(D-051). 학부모가 리포트를 열고 가장 먼저 읽는다. **한 번 만들면 그 주 내내 같은 문장**이 보인다 — 볼 때마다 바뀌면 무엇을 믿어야 할지 알 수 없다. `이번 달` 리포트에만 둔다(지난달에 '이번 주'는 뜻이 어긋난다).
  - AI에게는 **이미 계산된 숫자만** 넘긴다(`weekFacts`). 값이 없는 항목은 줄 자체를 만들지 않는다 — 모델이 `0`을 해석하지 않게. 프롬프트에 등급·백분위·예상 점수 금지를 못박는다.
  - **프롬프트만으로는 출력 형태를 통제할 수 없다.** 인사말을 붙이거나 문장마다 줄을 바꾸는 일이 실제로 일어난다(실측). 그래서 `tidySummary`로 **결정적으로 다듬는다**(인사 문장 제거·한 덩어리로 잇기·마크다운 제거·4문장 제한). 내용은 손대지 않는다.
  - 키가 없으면 같은 숫자로 만든 대체 문장을 쓰고 **AI가 쓴 글이 아니라고 밝힌다.** 프롬프트를 화면에 노출하지 않는다.
- **칭찬 스티커**는 학부모가 보내고 **학생 홈 맨 위 한 줄**로 받는다(§14). 종류는 자녀가 실제로 한 일과 이어진 것만 둔다(`꾸준히 했어요`·`과제를 다 냈어요`·`오답을 다시 봤어요`·`고마워요`) — `대단해요`처럼 근거 없는 칭찬은 자녀에게도 뜻이 없다. 자녀가 확인해 닫을 수 있다.
- **세부는 `자세히 보기`로 별도 화면**(`/parent/detail`)에 넘긴다. 휴대폰에서 한 화면에 다 펴면 무엇부터 볼지 알 수 없다. 핵심 리포트에 두는 것은 **판단에 바로 쓰이는 것**이고, 근거를 파고드는 값은 상세로 보낸다.
- **`이 리포트가 말하지 않는 것`을 상세 맨 아래에 둔다.** 또래·전국 평균, 문항별 역량, 읽기 속도, 답 변경 습관을 왜 두지 않는지 밝힌다. 없는 것을 있다고 오해하게 두지 않는 것도 화면의 일이다.
  - 지표 이름은 데이터가 실제로 말하는 것과 같아야 한다: 재풀이 결과를 저장하지 않으므로 `복습률`이 아니라 **`AI와 정리한 비율`**, `timeSec`이 벽시계 시간이므로 **`순공부 시간은 아니에요`**를 함께 적는다.
- **한 달이 리포트 하나다**(D-049). **누적 총합을 어디에도 두지 않는다** — 5년 쓴 학생의 `총 학습 시간 300시간`은 학부모가 이번 달에 무엇을 할지 아무것도 말해 주지 않는다. 모든 지표를 달로 끊고 **지난달과 나란히** 둔다(`지난달보다 1일 많아요`). 비교할 지난달 기록이 없으면 비교 문구를 만들지 않는다.
  - **머무는 지표는 `공부한 날 수`다.** 학습량 총합보다 학부모가 검증할 수 있는 사실이고, 오래 써도 뜻이 바래지 않는다.
  - **달 이동은 `‹ 7월 ›` 스테퍼다. 왼쪽이 과거, 오른쪽이 미래** — 방향이 곧 시간이라 순서를 외울 것이 없다. 갈 곳이 없으면 그 화살표를 막는다(44px 터치 영역).
    - **달을 선택 컨트롤로 늘어놓지 않는다**: 리포트는 달마다 하나씩 쌓여 5년이면 60개가 되고, 최신순으로 늘어놓으면 `7월` 오른쪽에 `6월`이 와서 시간 흐름과 반대가 된다.
    - **드롭다운도 쓰지 않는다**: 모바일에서 드롭다운은 조작이 느리고 오류가 늘어난다(리서치). 학부모가 가장 많이 하는 일은 "지난달 보기"이고 스테퍼는 그것이 한 번 누르기다.
    - 특정 달로 바로 가는 길은 `달마다 어떻게 변했나`가 맡는다(그쪽도 **오래된 달이 위**).
    - 기록이 있는 달만 오간다 — 빈 달로 이동해 아무것도 없는 화면을 보여 주지 않는다. `달마다 어떻게 변했나`는 최근 6개월의 공부한 날 수를 막대로 두고, 기록 없는 달도 `—`로 자리를 지킨다.
  - **날짜가 없는 기록은 어느 달에도 세지 않고 그 수를 밝힌다.** 임의의 달로 밀어 넣지 않는다.
- **`오답노트` 블록이 리포트의 핵심 중 하나다.** 정답률만으로는 "복습을 했는지" 알 수 없고, 그것이 학부모가 실제로 궁금해하는 것이다. 그 달에 담은 오답 · AI와 정리한 수 · 이해 완료 · 별표를 세고(`WrongNote.createdAt`) 자녀가 정리한 메모를 그대로 보여 준다.
- **탭마다 다른 이유가 있어야 한다.** `리포트`는 학습, **`자녀`는 이용권과 결제 주체**다(학부모가 대신 내주기). 자녀 탭에 리포트를 또 두면 두 탭이 같은 화면이 된다. 결제가 연결되지 않은 동안은 **표시만 남는다는 사실을 화면에 밝힌다** — 청구된 것처럼 보이게 하지 않는다.
- **홈과 리포트는 같은 계산을 쓴다.** 화면마다 집계를 다시 짜면 같은 자녀에 대해 서로 다른 사실을 말하게 된다(D-048의 근거). 새 지표가 필요하면 `buildChildReport`에 더하고 두 화면이 함께 읽는다.
- **순서는 학부모의 판단 순서다**: ① 아직 안 낸 학원 과제(달과 무관한 **지금** 상태) → ② 달 선택 → ③ 그 달 학습 → ④ 그 달 오답노트 → ⑤ 영역별 정답률 → ⑥ 그 달 학습 기록 → ⑦ 달마다 어떻게 변했나. 데이터를 만든 순서(집계 → 분해 → 원본)로 놓지 않는다. **행동을 맨 아래로 몰지 않는다** — 재풀이 요청은 학습 기록 각 행의 `trailing`이다.
- **같은 목록을 여러 번 나열하지 않는다.** 추이·상세·재풀이를 각각 섹션으로 두면 같은 학습이 세 번 나온다(그중 둘은 정렬이 반대였다). 목록은 하나다.
- **없는 숫자를 지어내지 않는다.**
  - 표본이 적으면 약점으로 단정하지 않는다. 문항 `WEAK_MIN_QUESTIONS`(20)개 미만이면 `문항이 적어 아직 판단하기 일러요`. 기준은 화면에 밝힌다(`문항 20개부터 약한 영역으로 봐요.`).
  - **근거 없는 안심 문구를 쓰지 않는다.** `고르게 하고 있어요` 같은 총평은 임계값 하나로 만든 평가다.
  - **마감일을 제출일 자리에 넣지 않는다.** 제출 시각이 없으면 `제출일 기록 없음`이라고 말하고 마감은 별도 행(`마감`)으로 둔다. 날짜가 없는 기록 수는 숨기지 않고 센다.
  - 고른 선지가 남아 있지 않은 제출은 **화면 위에서 한 번만** 밝히고(`어떤 선지를 골랐는지는 남아 있지 않아요`) 문항마다 `기록 없음`을 빨간 글씨로 반복하지 않는다.
- **출처는 항상 `SourceTag`**(§18). 학부모만 개인·학원 둘 다 보므로 지표에도 무엇을 합친 값인지 부제로 적는다(`학원 과제 4 · 개인 학습 2`).
- **지표는 `Table`(여러 대상 비교) 또는 `Group`+`Row`(한 대상의 소수 지표)**다. 손으로 metric 타일을 다시 그리지 않는다 — `flexBasis:'30%'`로 짜면 390에서 3칸이 유지되어 타일 안쪽이 74px가 된다.
- **목록 행에 세 가지를 동시에 두지 않는다.** `leading`(출처) + `meta`(정답률) + 라벨 있는 버튼을 함께 두면 390에서 본문이 한 글자 폭까지 밀린다. 행의 행동은 **아이콘 하나**로 좁히고 이름은 `accessibilityLabel`로 지킨다(결과 화면의 담기 토글과 같은 형태).
- **문장에 데이터를 끼울 때는 조사를 계산한다.** 영역 이름이 데이터에서 오므로 `독서이`가 된다. 받침 여부로 이/가를 고른다.
- 자녀가 정리한 메모는 `RichText` + 접기. 리포트가 메모로 덮이지 않게 한다.
- **자녀 리포트로 가는 길은 리포트 탭 하나다.** 홈·자녀 목록은 `/parent/report?child=…`로 보낸다. 화면 제목이 지금 누구를 보고 있는지 말한다(`정예린 님 리포트`).

## 20. 총괄관리자 화면 — 표와 출처

기준은 `app/admin/index.tsx`(D-070·D-076) · `academies.tsx` · `academy/[id].tsx` · `billing.tsx` · `content/index.tsx` · `content/[id].tsx`(D-068) · `users.tsx` · `user/[id].tsx` · `ops.tsx` · `metrics.tsx`(D-065·D-075). 컴포넌트는 `src/components/Table.tsx` · `SourceBadge.tsx` · `Sparkline.tsx` · `Stepper.tsx`.

- **지표를 카드로 만들지 않는다.** 여러 대상을 나란히 비교하는 자리는 **`Table`**이고, 한 대상의 소수 지표는 **`Group`+`Row` + 값은 `trailing`의 `label`**이다(§19의 학부모 리포트와 같은 형태). 값이 같은 x좌표에 서지 않으면 위아래로 훑으며 비교할 수 없다 — 타일이 늘어서면 전체 파악이 오히려 늦어진다.
  - **`Row.meta`에 값을 두지 않는다.** `inkTertiary`(대비 3.23:1, AA 미달)라 판단에 쓰는 값이 화면에서 가장 흐린 글자가 된다.
  - `StatTiles`는 **레포에서 사라졌다**(D-078). admin은 D-070, 학부모 리포트는 D-050, 학원 대시보드는 D-078에서 걷었고 마지막 사용처가 없어져 컴포넌트를 지웠다. 계정 목록의 규모는 필터 칸의 개수가 말한다.
- **숫자마다 출처를 밝힌다.** 지표 행은 값 옆 `SourceBadge`(`합성`·`실측`·`추정`), 표는 **표 바로 위에 열 묶음별 범례**를 둔다(`합성 좌석·활용률·활성·갱신` / `추정 월 청구액`). 셀마다 붙이면 배지가 값을 덮는다. 합성 집계와 세션 실측이 더해진 값에는 **배지를 둘 다** 붙이고 섞인 양을 부제에 적는다.
  - 배지 글자는 크기 스케일의 최소값(`xs 12`)을 쓴다. 신뢰도를 색이 아니라 글자로 말하는 유일한 장치라 스케일 밖으로 작게 두지 않는다(§4). 폭은 좌우 패딩(`spacing.xs`)으로 맞춘다.
- **열 규칙**: 숫자 열은 `align: 'right'`(등폭이 함께 적용된다), 이름 열은 폭을 고정하지 않고 남는 폭을 갖는다. 좁은 화면은 `priority`로 접는다 — **모바일은 1만, 태블릿은 1·2, 데스크톱은 전부**(D-074). 그래도 넘치면 **표 안에서만** 가로로 스크롤한다(§10). 본문 페이지는 가로로 스크롤하지 않는다.
  - **`minWidth`는 "전체 열을 다 펼쳤을 때의 하한"이다.** 표가 접힌 뒤에는 그 값과 실제로 필요한 폭(고정 폭 합 + gap + flex 열마다 88) 중 작은 쪽을 쓰고, 컬럼을 정말 넘칠 때만 스크롤한다. 접은 뒤에도 전체 기준 폭을 강제하면 이름 열이 부풀고 값이 화면 밖으로 밀린다.
  - **모든 열이 고정 폭이면 표가 내용 폭으로 줄어든다.** 늘어날 열이 없는데 컬럼을 다 차지하면 구분선만 빈 공간으로 뻗어 빈 셀처럼 읽힌다.
  - 헤더는 한 줄에 들어가는 길이로 줄이고(`최근 28일 활성` → `28일 활성`) 정의는 표 위 캡션에 적는다. 헤더가 접히면 정렬 화살표가 두 번째 줄로 끌려간다.
  - **값이 정책으로 자라는 열은 그 정책의 상한으로 폭을 잡는다.** 금액 열은 좌석 단가를 `PRICING_LIMITS.academySeat.max`까지 올린 값이 기준이다(실측: 학원 하나 `₩510,340,000` 104px · 합계 `₩597,120,000` 109px → 열 폭 132). `numberOfLines={1}`이라 부족하면 조용히 잘리고, 요금을 올려 보는 것이 그 화면의 목적이다(D-075).
- **`값`이 있으면 `추이`도 있다.** 추이 열 제목을 걸어 놓고 칸을 비우면 "추이가 없다"는 사실조차 전달되지 않는다.
  - 만들 수 없는 행의 추이 셀에는 **`추이 없음`이라고 글자로 적는다**(DAU·고착도처럼 지금 값만 있는 지표).
  - **표 전체에 추이가 없으면 `변화`·`추이` 열을 만들지 않는다** — 빈 칸 두 줄은 정보가 아니라 잡음이다. 열 배열을 `rows.some((r) => r.values.length > 1)`으로 조립한다(적재용량 표).
  - **`변화`의 `—`는 뜻을 표 아래 캡션이 밝힌다**(`지금 값 하나만 있는 지표는 비교할 주가 없어 —로 둬요`). `—`와 `0`이 같은 열에 섞이는데 화면에 설명이 없으면 둘을 구별할 수 없다.
  - **변화가 0일 때도 단위를 붙인다**(`0%p`·`0곳`·`₩0`). 단위 없는 `0`은 비율인지 사람 수인지 알 수 없다. 값과 마찬가지로 `inkTertiary`를 쓰지 않는다.
  - **stock과 flow를 다르게 자른다.** 흐름(그 주에 활동한 사람)은 완성된 주까지만 그리고, 재고(그 주까지 가입한 계정 수)는 기준일이 속한 주까지 as-of로 그린다 — 그래서 **마지막 점이 화면의 `값`과 정확히 같다**(D-076). 두 값이 어긋나면 표 한 줄이 자기 안에서 다른 말을 한다.
  - 추이 열은 `priority: 2`다. 모바일에서는 접히고 `변화`가 남는다 — 84px 열을 하나 더 펼치면 표가 가로로 스크롤한다.
- **기간 토글은 하나다.** 화면에 기간 선택이 있으면 **첫 줄 고지에 주 수를 적지 않는다**(`2026-07-28 기준`) — `4주`를 고른 화면과 어긋난다. 토글이 닿지 않는 블록은 **섹션 제목에 기간을 못박는다**(`주간 코호트 잔존 (최근 12개 코호트 · 경과 9주)` · `주 중 학습일 분포 (최근 7일)` · `적재용량 (최근 4주 유입·이탈 기준)`).
- **분모를 지우지 않는다.** 분포 막대에서 가장 큰 집단(최근 7일에 활동이 0일인 학생)을 빼면 "분산을 드러낸다"는 말 자체가 성립하지 않는다. 버킷을 남기고 전체 수를 캡션에 적는다.
- **계절성을 화면이 말한다.** 학습 제품의 추이는 방학·시험으로 크게 움직인다. 추이 블록에는 `src/data/calendar.ts`의 **가정한 학사 일정**을 읽어 지금 보이는 기간의 이벤트를 한 줄로 적는다. `Sparkline`에 마커를 그리지 않는다 — 글자 한 개 크기의 그림에 축과 라벨을 넣지 않는다는 규칙이 먼저다.
- **`sparkLabel`은 마지막 점을 `지금`이라고 부르지 않는다**(D-089). 추이는 끝난 주까지만 그리므로 마지막 점은 지난주 값이다 — 지금 값은 화면 위의 지표가 말한다. 점 하나가 무엇인지(`주`·`번`)도 호출부가 정한다: 낸 순서로 그리는 추이를 `주`로 읽으면 안 된다.
- **임계값은 색이 아니라 글자로도 말한다.** Quick Ratio 1을 색으로만 가르지 않고 값 아래에 `1 이상`·`1 미만`을 적는다(§11). 색은 그 위에 얹는 보조 신호다.
- **지표 설명은 화면 어투다.** `MetricDef.desc`·`fake`는 개요와 지표 사전에 그대로 나가므로 `-어요`로 쓴다. 어투를 두 벌 두지 않는다 — 사전 어투가 필요한 독자는 없다. `formula`는 명사구라 그대로 둔다.
- **표가 없는 상세 화면은 `wide`를 쓰지 않는다.** `Group`+`Row`만 있는 화면에서 폭이 960이면 행 제목과 `trailing` 값이 960px 떨어져 위아래로 훑기가 오히려 어렵고, `Field`도 960px 폭 입력이 된다(`/admin/user/[id]`).
- **정렬은 열 헤더가 맡는다.** 정렬용 선택 컨트롤을 따로 두지 않는다 — 목록을 걸러내는 것은 `SegmentedControl`, 같은 목록을 다르게 줄 세우는 것은 헤더다(§8). **기본 정렬은 운영자가 먼저 봐야 할 것**이다(학원 목록 = 갱신 임박순, 반 목록 = 제출률 낮은 순).
- **표에 페이지 슬라이스를 넘기면 정렬을 화면이 쥔다**(`useTableSort`, D-086). 표가 스스로 정렬하면 **받은 배열**, 즉 그 페이지 안에서만 줄이 바뀌어 `열 이름을 누르면 다시 정렬해요`가 거짓이 된다. 비교 함수는 화면마다 `COMPARE` 맵 하나이고 컬럼의 `sort`와 훅이 **같은 값을 가리킨다** — 두 벌이면 헤더는 눌리는데 다른 기준으로 돈다. 정렬을 바꾸면 페이지를 처음으로 되돌린다.
  - **`Column.sort`는 오름차순으로 정의한다. 내림차순은 표가 뒤집는다.** `b - a`로 주면 첫 클릭이 화살표와 반대 순서를 낸다(A-076).
  - 화살표는 **정렬 중인 열에만** 둔다(`arrow-up`=오름 · `arrow-down`=내림). 정렬할 수 있다는 사실은 헤더가 버튼이라는 것과 이름이 말한다. 활성 헤더는 색 밖에서도 굵기와 `aria-selected`로 말한다(§11).
  - 헤더의 누름 영역은 44px이다 — 캡션 한 줄에 세로 패딩을 더하고 늘어난 만큼 음수 마진으로 되돌려 헤더 행 높이는 그대로 둔다(`BackLink`와 같은 방법). `hitSlop`은 react-native-web의 `Pressable`에서 동작하지 않는다.
- **펼침 행에는 표시와 상태를 함께 둔다.** `expand`가 있는 표는 마지막에 아래·위 chevron을 두고(이동을 뜻하는 `chevron-right`와 방향이 갈려야 한다, §8) `aria-expanded`를 주며, 펼친 내용은 첫 열 폭만큼 들여쓴다. 들여쓰기가 없으면 새 행처럼 읽힌다. 한 번에 한 행만 열린다.
- **노드를 반환하는 열이 있으면 `rowLabel`을 준다.** 조립 라벨은 노드(추이선·출처 배지·색이 들어간 값)를 읽지 못하고 `열이름 (값을 읽을 수 없음)`으로만 적는다 — 빠진 열이 있다는 사실 자체를 남기려는 것이지, 그 상태로 두라는 뜻이 아니다.
  - `rowLabel`은 셀과 **같은 말**을 해야 한다. 변화·추이·임계 판정을 만드는 함수를 하나 두고 셀과 라벨이 함께 부른다(개요의 `deltaText`·`quickRatioText`) — 두 곳에서 따로 문장을 만들면 화면과 스크린리더가 갈린다.
- **경고는 색이 아니라 글자다.** 좌석 활용률 60% 미만은 이름 아래 `활용률 낮음`, 오답률 70% 이상 문항은 그 행에 `해설을 다시 볼 문항이에요`를 붙인다(§11).
- **값을 오르내리는 컨트롤은 `Stepper`**(44px · `atMin`/`atMax`)다. 손으로 그리면 터치 타깃이 무너진다(D-064가 같은 판단). 값 노드에는 서식된 문자열 하나만 둔다 — 단위나 보조 문구를 함께 넣으면 값을 그대로 확인하는 테스트가 깨진다.
- **집계는 화면에서 다시 계산하지 않는다.** 매출은 `src/features/revenue.ts`, 운영 지표는 `src/features/adminMetrics.ts`, 반별 수행률·가중 정답률은 `src/features/academyStats.ts`에서 온다. 같은 이름의 값이 화면마다 다르면 어느 쪽도 믿을 수 없다(D-048·D-052·D-061이 같은 결함을 세 번 고쳤다).
- **합계 행은 셀 수 있는 열을 다 채운다.** 좌석 합계를 비워 두면 표가 그 값을 셀 곳이 없다고 말하는 셈이다. 비율·상태처럼 더할 수 없는 열만 비운다.
- **같은 목록을 두 번 나열하지 않는다**(§19와 같은 규칙). 콘텐츠 상세의 문항별 오답률은 **한 목록**이고 오답률 내림차순으로 상위 5문항만 보인 뒤 `N개 더 보기`로 펼친다 — `어려운 문항 상위 5`를 따로 두면 앞 목록의 부분집합이 같은 문구로 다시 온다(D-075).
- **쿼리로 좁혀 보낸 화면은 좁힌 이유와 되돌아갈 길을 말한다.** 개요의 알림은 목적지를 좁혀 보내고(`/admin/content?wrong=70`) 받는 화면은 ① 좁힌 조건과 규모를 한 줄로 ② `전체 목록 보기`로 `replace` ③ 좁힌 근거를 값으로 보여 주는 열을 그동안만 둔다(그 열은 모바일에서도 접지 않는다). 범위를 벗어난 쿼리는 무시하고 전체를 보여 준다 — 주소를 손으로 고쳤을 때 빈 화면이 되지 않게(D-075).
- **선택지가 하나뿐인 필터는 렌더하지 않는다**(§8의 "0건 칸" 규칙의 연장). 기록 0건인 새 세션에 `전체 0` 하나만 남으면 이미 선택된 채 놓인 선택 컨트롤이 된다. 개수는 섹션 제목이 말한다(D-075).
- **대리 보기는 무엇이 열리는지 시작 전에 말한다**(D-149). `/admin/user/[id]`의 대리 보기 섹션은 `지금 열리는 범위`를 대상 역할에서 계산해 문장으로 두고, 같은 문장이 감사 로그의 `열람 범위:`에 남는다(`src/features/impersonation.ts` 한곳에서 만든다 — 두 곳에서 쓰면 화면과 기록이 갈린다).
  - **사유 유형은 대상 역할마다 다르다.** 남의 기록이 함께 열리는 대상(학부모·학원)에는 `데이터 점검`을 두지 않고 **문의 번호를 필수로** 받는다 — 특정 문의나 오류 없이 자녀·학원 기록까지 넓히지 않는다(최소권한). 학생 대상은 세 유형 그대로이고 문의 번호는 선택이다.
  - 대상을 학생으로 제한하지는 않는다 — `학부모가 자녀 리포트를 못 본다`는 문의를 재현할 수 없게 된다(M9-08 ②를 고르지 않은 이유).
- **탭 화면에는 뒤로가기를 두지 않는다**(`backFallback`은 상세 화면만). 한글 `eyebrow`도 두지 않는다(§4).
- **전폭 버튼은 화면의 목적을 끝내는 것 하나뿐이다.** `새 문제 등록하기`는 목록 섹션 제목 옆의 `hug` + `size="sm"`이다(D-047).
- **등록을 마치면 방금 만든 것을 보여 준다.** 운영자 문제 등록의 완료 버튼은 `등록한 문제 보기`이고 목적지는 그 세트 상세다(학원은 `이어서 배정하기` → 배정, D-064). 개요로 보내면 방금 만든 것이 없는 화면으로 튄다.

## 부록 A. 현재 코드 감사 — 바꿔야 할 점

- 팔레트가 **차가운 회색(GitHub #f6f8fa/#d0d7de)** → Notion식 **따뜻한 중립**(§3 값)으로 조정. 다크도 웜그레이.
- 지문 컴포넌트 컬러 왼쪽 바 + 채운 박스 제거 완료 → 기사형 유지(§13).
- `ScoreCard` 거대 숫자 → metric+막대 완료. `QuestionReview` pill → 점+텍스트 완료.
- 학생 홈 히어로 계층 적용 완료. §14의 "개인 학습" 섹션을 홈에도 노출(현재 과제·오답복습 위주).
- `Field` 높이 44~48 정렬, radius md 확인.
- 학원 상세·학부모 리포트도 metric/hairline 원칙으로 통일 점검.

---

## 부록 B. Toss 영향 & 아이콘 시스템 (리서치 정리)

### 토스스러움이란 (리서치 요약)
- **극단적 단순함·직관성**을 창립 원칙으로. 한 화면 = 한 작업.
- TDS(토스 디자인 시스템) 핵심 컴포넌트: **BottomCTA**(전폭 하단 주요 버튼), **ListRow**(아이콘+텍스트+우측 값), **Tab/Navigation**, **Asset**(아주 단순한 아이콘), **Badge/Border/Paragraph**.
- **Toss Blue 단색 강조** + 흰 배경 + 큰 굵은 타이포(숫자 강조).
- 친근한 **존댓말 마이크로카피**("~할게요/~돼요/잠깐이면 돼요"), 안심시키는 톤.
- 부드러운 마이크로 인터랙션은 **사용성 다음**. 장식보다 명료함.
- 출처: TDS 공식(developers-apps-in-toss.toss.im/design), Toss 10년 UX 진화(Medium).

### 스코디 적용 (재조합: Toss 명료함 + Linear/Notion 절제)
- **Accent = Toss Blue** `#3182f6`(라이트)/`#4d90fe`(다크). **단색만**, 그라데이션 금지. 주요 버튼·선택·활성 탭·링크에만.
- **주요 버튼**은 화면당 하나, 눈에 띄게(BottomCTA 감성). 나머지는 secondary/ghost.
- **ListRow**: 필요 시 leading 아이콘 + 타이틀/서브 + 우측 meta/chevron.
- radius는 moderate(md 8, lg 12) — 토스의 부드러움과 절제 사이. 큰 라운드(16+)·큰 그림자는 여전히 금지.

### 아이콘 시스템 (`src/components/Icon.tsx`)
- 라이브러리: **Feather**(@expo/vector-icons) — 아주 단순한 단일 스트로크 라인 아이콘. 토스 Asset의 "간단한 느낌"에 부합.
- 크기: 탭바 22, 사이드바 18, 행 chevron 18. 색: 활성=accent, 비활성=inkSecondary/inkTertiary. 단색.
- 의미 매핑: 홈 `home`, 학습/콘텐츠 `book-open`/`file-text`, 기록/리포트/분석 `bar-chart-2`, 내 정보 `user`, 자녀/반·학생 `users`, 대시보드 `grid`, 학습 배정 `edit-3`, 학원 관리 `settings`, 이동 `chevron-right`, 추가 `plus`, 정답 `check`, 별표 `star`, **노트 정리 `file-plus` → 정리됨 `check-square`**.
- **카드 헤더의 인라인 액션은 16px**(오답노트의 노트·별표·휴지통). 탭바 22·사이드바 18과 다른 이 화면의 기준이다. 셋을 나란히 둘 때는 전부 `Icon`으로 맞춘다 — 하나만 텍스트 글리프(`★`)면 스트로크 굵기가 어긋난다.
- 규칙: **의미 있는 곳에만**. 장식용 아이콘 남발 금지(§13). 색만으로 의미 전달 금지 — 라벨 병행.
