스코디(Scody)는 고등학생 국어 학습 플랫폼이다.

기술 스택: Expo · React Native · React Native Web · Expo Router · TypeScript.

현재는 사용자 흐름을 검증한 프로토타입이다. 실제 인증, 영속 데이터베이스, 결제, 운영 보안은 미완성이다.

## 문서

- `CLAUDE.md`: 항상 적용하는 규칙
- `docs/SCODY_MASTER_PLAN.md`: 확정 정책, 현재 상태, 남은 작업
- `docs/archive/SCODY_WORKLOG_2026-07.md`: 진행 기록

충돌하면 ①확정 정책 ②기존 코드와 테스트가 보여 주는 실제 동작 ③오래된 기록 순으로 판단한다. 해결할 수 없으면 구현하지 말고 `BLOCKED`로 보고한다.

## 작업 규칙

1. 작업 전 `docs/SCODY_MASTER_PLAN.md`를 읽는다.
2. 사용자가 지정한 범위만 수행한다. 한 번에 문제 하나 또는 연결된 흐름 하나.
3. 구현 전 실제 화면·라우트·데이터·테스트를 확인한다. 문서만 보고 없는 문제를 추측하지 않는다.
4. 확정 정책(마스터 플랜 2절)은 임의로 바꾸지 않는다. 필요하면 `결정 대기`에 기록한다.
5. 개인 학습과 학원 학습의 출처·권한이 섞이지 않게 한다.
6. 기존 테스트를 삭제·skip·완화하거나 오류를 숨겨서 완료하지 않는다.
7. 요청과 무관한 리팩터링, 패키지 교체, 폴더 개편을 하지 않는다.
8. 실패·미구현 상태를 숨기지 않는다.
9. 작업 후 마스터 플랜의 남은 작업과 워크로그를 갱신한다. 검증 근거 없이 완료 표시하지 않는다.

## 데이터·인증·보안

- 테스트 fixture와 운영 데이터를 구분한다. 하드코딩 데이터는 테스트·개발 seed에서만 쓴다.
- UI가 `src/data` 접근 경계를 우회하지 않게 한다.
- 인증·권한을 화면 숨김만으로 판단하지 않는다. 운영 데이터 연결 시 서버·DB 정책에서도 검증한다.
- 비밀번호·토큰·API 키·개인정보를 코드·로그·fixture에 넣지 않는다. `EXPO_PUBLIC_*`은 공개 정보로 간주한다.
- OpenRouter 비밀키를 운영 클라이언트 번들에 포함하지 않는다.
- 실제 사용자 데이터 삭제, 운영 배포, 결제 활성화는 자동 수행하지 않는다.
- fixture를 실제 사용자 데이터처럼 표시하거나 운영 준비 완료로 표현하지 않는다.

## 내비게이션

- 상세·단계형 화면에는 예측 가능한 이탈 경로가 있어야 한다(`Screen`의 `backFallback`).
- 네이티브는 시스템 뒤로가기와 Expo Router 스택 동작을 우선한다. 웹은 브라우저 뒤로가기와 직접 URL 진입을 함께 고려한다.
- 히스토리 없는 직접 진입은 안전한 역할별 상위 경로로 보낸다. 세션이 없으면 로그인으로 가드한다.
- 상위 탭 화면에 의미 없는 뒤로가기를 일괄 추가하지 않는다.
- 새 라우트에는 정상 진입·직접 URL 진입·뒤로가기를 테스트한다.

## 디자인

토스의 자산이나 화면을 복제하지 않고 UX 원칙만 참고한다: 친근하고 짧은 한국어, 충분한 여백, 큰 제목과 명확한 위계, 화면당 하나의 주요 행동, 큰 둥근 모서리와 절제된 그림자, 모바일에서 쉬운 조작, 복잡한 상태를 안심되는 문구로 설명.

말투는 Toss UX 라이팅을 따른다. 존댓말 `-어요`, 한 문장에 한 가지, 겁주거나 재촉하지 않기, 과한 칭찬·느낌표·이모지 금지.

브랜드 강조색은 Scody 청록(마스터 플랜 D-002).

- 팔레트 기준은 `src/theme/palette.ts`, 컴포넌트에서는 `colors.*` 의미 토큰으로 참조
- 임의의 색상 리터럴 추가 금지. 네온·그라데이션·글래스·발광 금지
- 본문·제목은 Pretendard(레포 번들 `assets/fonts`, OFL). Space Grotesk는 `Scody` 워드마크에만 쓴다
- 디자인 변경 시 모바일·태블릿·데스크톱을 확인한다

피해야 할 AI 생성형 디자인: 모든 내용을 카드로 감싸기, 아이콘+제목+설명이 똑같이 반복되는 섹션, 의미 없는 배지·수치·그래프, 보라·파랑 그라데이션, 반짝이·로봇 아이콘, "AI가 분석했어요" 같은 상투적 문구, 근거 없는 칭찬·추천 수치, 장식이 내용보다 먼저 보이는 화면.

카드 제거가 목표는 아니다. 카드는 정보의 소속·선택·상호작용을 설명할 때만 쓰고, 단순한 구분은 여백·타이포그래피·구분선·리스트 그룹을 우선한다.

## 구조

- `app/`: Expo Router 라우트
- `src/theme/`: 팔레트, 토큰, 반응형
- `src/components/`: 공통 UI
- `src/data/`: 도메인 타입, fixture, 로스터, 콘텐츠, 분류, 접근 경계
- `src/features/`: 도메인 상태(학습·진행·콘텐츠·학원 인사·AI 호출·요금 정책·감사 로그)
- `src/session/session.tsx`: 인증·세션 교체 경계
- `e2e/`: Playwright 사용자 흐름 테스트

문서의 경로와 실제 코드가 다르면 코드를 확인하고 문서를 고친다. 없는 경로·API를 추측해 쓰지 않는다.

## 검증

| 목적 | 명령 |
|---|---|
| 웹 개발 서버 | `npm run web` |
| 타입 검사 | `npm run typecheck` |
| 린트 | `npm run lint` |
| 단위·통합 테스트 | `npm test` |
| Playwright | `npm run test:e2e` |

- 테스트는 화면에 보이는 텍스트·역할·버튼·URL 기준으로 쓴다. CSS 클래스나 내부 함수에 결합하지 않는다.
- 각 E2E는 독립 실행 가능해야 한다. 라우팅·인증·권한 변경은 관련 E2E를 반드시 실행한다.
- 핵심 화면은 모바일·태블릿·데스크톱에서 확인한다.
- RNTL v14의 `render`는 비동기다. `await render(...)`를 쓴다.
- 기존 실패가 있으면 기준 실패와 새 회귀를 구분해 기록한다.
- **`EXPO_PUBLIC_*`을 바꿔 빌드한 뒤에는 `dist`를 `--clear`로 다시 만든다.** Metro 캐시는 출력
  디렉터리를 갈라도 공유된다 — 꺼진 빌드 대조(개발용 로그인 스위치)를 한 뒤 그냥 `web:export`하면
  **모듈 하나만 꺼진 산출물**이 나온다. 증상이 코드 결함처럼 보인다: 실측(2026-08-20)으로 계정
  배열만 빈 `dist`가 만들어져 E2E 5건이 결정적으로 깨졌고, 같은 파일을 단독으로 돌리면 통과했다.

## 완료 기준

- 승인된 완료 조건 충족
- 변경 범위가 목적에 맞게 작음/
- 타입 검사·린트·관련 테스트 통과
- UI 변경은 관련 뷰포트에서 확인
- 권한 변경은 허용·거부 경로 모두 확인
- fixture나 우회 코드로 테스트만 통과시키지 않음
- 마스터 플랜과 워크로그에 실제 검증 결과 기록

## 화면을 만들 때 확인할 것

1. 이 화면의 사용자는 누구이고, 왜 왔는가?
2. 가장 먼저 이해하고 실행할 행동은 무엇인가?
3. 취소하거나 이전 단계로 돌아갈 수 있는가?
4. 로딩·빈 상태·오류·권한 없음·직접 URL 진입은 어떻게 보이는가?
5. 행동이 완료됐다는 사실을 알 수 있고, 다음 행동으로 이어지는가?
6. 개인 학습과 학원 학습의 출처·공개 범위가 명확한가?
7. 테스트 데이터를 실제 상태로 오해하게 만들지 않는가?
8. 모바일·태블릿·데스크톱에서 같은 우선순위가 유지되는가?
