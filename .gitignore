node_modules/
.expo/
dist/
web-build/
*.log
.DS_Store
playwright-report/
test-results/
expo-env.d.ts
.env
# 웹 폰트 서브셋을 만들 때만 쓰는 파이썬 가상환경(scripts/build-web-fonts.py)
.fontenv/

# Lottie 렌더러(1.79MB). `npm run motion:wasm`으로 만든다 — 레포에 커밋하지 않는다.
public/dotlottie-player.wasm

# Playwright MCP가 브라우저 확인 중에 남기는 스냅샷·콘솔 로그
.playwright-mcp/

# 개발 seed(`npm run db:seed`가 만든다). 실행마다 난수 초대 토큰이 들어가므로
# 레포에 올리지 않는다 — `scripts/gen-seed.ts`가 그것을 전제한다(A-103·D-137).
supabase/seed.sql
.vercel
.env*
